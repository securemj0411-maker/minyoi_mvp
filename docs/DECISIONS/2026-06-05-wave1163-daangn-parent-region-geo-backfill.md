# Wave 1163 — 당근 행정동 거리 좌표 보강

- 결정: `src/lib/generated/daangn-region-geo.json`을 기존 구/시드 중심 좌표 385개 수준에서 당근 parent 행정동 좌표까지 보강했다.
- 이유: 사당동 기준 피드에서 `관악구 신사동` 같은 일부 동네가 실제 행정동 중심이 아니라 `서울 관악구` 구 중심점으로 fallback 되어 `약 2km`처럼 잘못 표시되는 문제가 있었다.
- 검증:
  - 좌표 생성 결과: `ok=6054`, `failed=5`, `total=6439`.
  - 남은 miss: 옛/통폐합 행정동명 5개(`장전제3동`, `안심3.4동`, `송산동`, `공단1동`, `삼동동`).
  - 사당동 기준 재계산: 관악구 신사동 `4.9km`, 남현동 `1.1km`, 낙성대동 `1.6km`, 봉천동 `1.9km`, 동작구 상도동 `3.4km`.
  - `npm run lint` 에러 0개(기존 warning 유지), `npm run build` 성공.
- 보류:
  - 남은 5개 miss 행정동은 현재 피드 영향이 작아서 별도 명칭 alias 보강으로 후속 처리한다.
  - 사용자에게 거리 표기가 행정동 중심 기준이라는 보조 설명을 넣을지는 UX 부담을 보고 별도 판단한다.
