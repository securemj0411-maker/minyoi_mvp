# 2026-06-05 Wave 1142 — 모바일 피드 필터 바텀시트

## 결정
- 모바일 추천 피드 상단에서 예산/위치/출처/정렬 select 4개를 계속 노출하지 않고, `필터` 버튼 1개로 접는다.
- 버튼에는 현재 적용된 조건을 요약해 보여주고, 탭하면 하단 바텀시트에서 큰 터치 영역으로 조건을 고르게 한다.
- 데스크톱은 기존 select 4개를 그대로 유지한다.

## 구현
- `src/components/explore-client.tsx`
  - 모바일 전용 필터 요약 버튼 추가.
  - 모바일 전용 필터 바텀시트 추가.
  - 기존 예산/위치/출처/정렬 변경 로직을 공용 핸들러로 분리해 데스크톱 select와 모바일 버튼이 같은 동작을 쓰게 했다.

## 보류
- 로그인 세션이 없는 로컬 브라우저에서는 `/me` 본문 피드를 시각 검증하지 못했다. 이번 변경은 lint와 정적 렌더링 구조 기준으로 검증했다.
