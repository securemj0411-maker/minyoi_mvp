# 2026-05-20 — wave396 PG 심사 사업자 정보/크레딧 정책 반영

## 결정

- PG 심사 준비를 위해 사이트 푸터의 mock 사업자 정보를 실제 제공값으로 교체했다.
  - 상호: `득템잡이`
  - 사업자등록번호: `563-62-00789`
  - 주소: `제주특별자치도 제주시 수덕로 75, 302호`
  - 대표번호: `010-8168-5816`
  - 고객센터 이메일: `mj12270411@gmail.com`
- 통신판매업 신고는 사용자 설명대로 PG 신청 이후 진행 예정이므로 푸터에는 `신고 준비 중`으로 표기한다.
- 기존 코드에 있던 대표자명 `이민제`는 유지했다. 사업자등록증상 대표자명이 다르면 즉시 교체해야 한다.
- 법정책 페이지 상단의 `MVP 검토용 mock` 문구를 제거하고 실제 서비스 정책 문서 톤으로 변경했다.
- 득템잡이 결제 구조를 구독형이 아니라 크레딧 충전형으로 정리했다.
  - `/plans`는 `요금제/구독` 대신 `크레딧 패키지/단건 충전` 톤으로 변경한다.
  - `/billing/checkout`은 `구독 상품` 대신 `크레딧 충전 상품`, `신용카드 일반결제`, `자동 갱신 없음`으로 표시한다.
  - 환불정책의 정기 구독 환불 조항은 `크레딧 충전 및 잔여 크레딧 환불 기준`으로 교체한다.
- 개인정보처리방침에는 결제 위탁 업체 후보로 `포트원`, `NHN KCP`, `KG이니시스`를 반영했다.

## 보류

- 실제 통신판매업 신고번호는 아직 없다. 신고 완료 후 푸터/정책 페이지에 신고번호를 교체해야 한다.
- 실제 PG 계약 완료 후 최종 결제대행사가 달라지면 개인정보처리방침의 결제 위탁 업체 목록을 다시 갱신해야 한다.
- 내부 API/DB 명칭에는 아직 `plan`, `subscription` 용어가 남아 있다. 이번 작업은 PG 심사에 노출되는 사용자-facing 문구와 정책 정리를 우선했다.

## 검증

- `rg`로 옛 mock 사업자번호, 옛 이메일, 대표자명 placeholder, mock 정책 문구가 공개 UI/정책 파일에 남아 있지 않음을 확인했다.
- `npm run lint --`로 변경된 공개 UI/정책 파일 lint 통과.
- Node `fetch`로 `/`, `/plans`, `/terms`, `/privacy`, `/refund-policy`, `/youth-policy` 응답 200 확인.
- `git diff --check` 통과.
