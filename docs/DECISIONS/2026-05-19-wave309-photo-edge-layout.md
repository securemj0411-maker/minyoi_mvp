# 2026-05-19 wave309 - 상세 사진과 본문 패딩 분리

## 결정
- /me 상세 모바일 스크롤 컨테이너의 좌우 패딩을 제거했다.
- 상품 사진은 `w-screen` 풀블리드 영역에 그대로 두고, 사진 아래 텍스트/요약 영역에만 `px-3`을 적용했다.
- 사진 foreground는 다시 `object-contain` + 약한 scale로 되돌렸다. 상품 자체를 자르지 않기 위해서다.
- 시세 그래프 카드는 모바일에서 별도 `mx-3`을 적용해 본문 카드만 안전 여백을 갖게 했다.

## 보류
- 모바일 사진의 원본 비율 때문에 실제 상품 이미지 주변에 배경이 보일 수는 있다. 이번 변경의 목표는 사진 섹션 자체가 화면 좌우 끝까지 붙는 것이다.
