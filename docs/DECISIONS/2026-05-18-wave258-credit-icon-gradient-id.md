# 2026-05-18 wave258 — 크레딧 엽전 금색 렌더링 고정

## 결정
- `CreditIcon`의 SVG gradient id를 인스턴스별 고유 id로 바꿨다.
- 모바일/데스크톱 네비에 같은 아이콘이 여러 개 렌더될 때 `url(#gradient-id)` 충돌로 PC 엽전 색이 죽는 문제를 막는다.

## 보류
- 크레딧 아이콘의 형태와 색상 팔레트 자체는 유지했다.
- 크레딧 구매/구독 정책은 변경하지 않았다.
