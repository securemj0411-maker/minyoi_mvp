# 2026-05-12 Wave 1 Decisions

## v2.217 - speaker report packet registry pilot

- 결정: `speaker` category도 `scripts/lib/report-packet-registry.ts`에 편입했다. 이제 `smartwatch + earphone + headphone + monitor + desktop + game-console + camera + speaker`가 모두 registry-first 구조로 readiness / category evidence ledger / report-only audit / manifest audit에 연결된다.
- 결정: `speakerReadinessSteps`, `speakerLatestPhaseFiles`, `speakerManifestFiles`, `speakerCategoryEvidenceSpecs`를 만들고, 기존 chain 4종에서 speaker literal registration을 제거했다.
- 결과:
  - `reports/parser-registry-pilot-status-latest.md`
  - `registry pilot files=33`
  - `fully_registry_backed=33`
  - `remaining_literals=0`
  - `reports/parser-manifest-audit-latest.md`
  - `status=ok; checked=123; missing=0`
- 보류:
  - `home-appliance`는 아직 registry pilot 밖에 둔다.
  - runtime parser wiring, candidate pool policy wiring, public promotion, generic packet runner/spec compiler는 계속 보류한다.
  - 이번 단계는 report-only orchestration 중복 제거와 유지비 절감까지만 수행한다.
- 다음:
  1. `home-appliance`를 다음 registry pilot 후보로 검토한다.
  2. registry spec에 optional tags / phase grouping / notes를 추가해 family별 묶음을 더 쉽게 다루게 한다.
  3. 이후 새 packet 추가는 pilot 범위 family/category에서 registry-first 규칙으로만 진행한다.

## v2.218 - home-appliance report packet registry pilot

- 결정: `home-appliance` category도 `scripts/lib/report-packet-registry.ts`에 편입했다. 이제 `smartwatch + earphone + headphone + monitor + desktop + game-console + camera + speaker + home-appliance`가 모두 registry-first 구조로 readiness / category evidence ledger / report-only audit / manifest audit에 연결된다.
- 결정: `homeApplianceReadinessSteps`, `homeApplianceLatestPhaseFiles`, `homeApplianceManifestFiles`, `homeApplianceCategoryEvidenceSpecs`를 만들고, 기존 chain 4종에서 home-appliance literal registration을 제거했다.
- 결과:
  - `reports/parser-registry-pilot-status-latest.md`
  - `registry pilot files=37`
  - `fully_registry_backed=37`
  - `remaining_literals=0`
  - `reports/parser-manifest-audit-latest.md`
  - `status=ok; checked=123; missing=0`
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion, generic packet runner/spec compiler는 계속 보류한다.
  - 이번 단계는 report-only orchestration 중복 제거와 유지비 절감까지만 수행한다.
- 다음:
  1. registry spec에 optional tags / phase grouping / notes를 추가해 family별 묶음을 더 쉽게 다루게 한다.
  2. registry에서 generic packet runner/spec compiler로 더 줄일 수 있는지 검토한다.
  3. 이후 새 packet 추가는 pilot 범위 family/category에서 registry-first 규칙으로만 진행한다.

## v2.219 - registry metadata layer

- 결정: `scripts/lib/report-packet-registry.ts`에 family/category 단위 메타 레이어를 추가했다. 이제 registry는 단순 파일/명령 나열이 아니라 `phase`, `tags`, `notes`를 가진 `registryPacketGroups`를 함께 제공한다.
- 결정: `smartwatch`, `earphone`, `headphone`, `monitor`, `desktop`, `game-console`, `camera`, `speaker`, `home-appliance` 9개 group을 metadata-first로 정의했다.
- 결정: metadata coverage를 별도 구조 리포트로 확인할 수 있게 했다.
  - `scripts/report-parser-registry-metadata-status.ts`
  - `reports/parser-registry-metadata-status-latest.md`
- 결과:
  - `reports/parser-registry-pilot-status-latest.md`
  - `registry pilot files=37`
  - `fully_registry_backed=37`
  - `remaining_literals=0`
  - `reports/parser-registry-metadata-status-latest.md`
  - `groups=9`
  - `fully_annotated_groups=9`
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion은 계속 보류한다.
  - generic packet runner/spec compiler는 아직 만들지 않고, metadata-first registry까지만 올린다.
  - 이번 단계는 report-only orchestration 의미 구조화와 다음 자동화 준비까지만 수행한다.
- 다음:
  1. metadata layer를 바탕으로 `generic packet runner/spec compiler` 후보 구조를 설계한다.
  2. `phase`/`tags` 기준 packet 묶음 요약 리포트를 늘려 backlog 정리를 더 자동화한다.
  3. 이후 새 packet 추가는 pilot 범위 family/category에서 registry-first + metadata-first 규칙으로만 진행한다.

## v2.220 - conflict-safe no-mutation sampling batch

- 결정: 다른 작업자가 runtime / Supabase / cron / pack UI / `30일_실행계획.md`를 만지고 있으므로, 이번 batch는 `scripts/report-*`, `reports/*`, `docs/DECISIONS/*` 범위의 report-only 작업으로 제한했다.
- 결과:
  - `reports/headphone-non-airpods-real-raw-scope-latest.md`
  - `rawFetchedUnique=42`, `candidatePositive=0`, `candidateRiskRows=0`
  - `reports/game-console-no-mutation-runtime-dry-run-latest.md`
  - `rows=14`, `failedRows=0`, `positive/manual/hold=4/5/5`
  - `reports/game-console-real-raw-scope-latest.md`
  - `rawRows=0`, `candidateRiskRows=0`
- 해석:
  - non-AirPods headphone은 현재 runtime 확장 근거가 아니라 더 넓은 sampling / guardrail review가 필요하다.
  - game-console fixture dry-run은 통과했지만, 실제 raw sample이 0건이라 live 시장 근거로는 부족하다.
  - 이번 결과는 runtime 승인이 아니라 no-mutation baseline으로만 사용한다.
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion, DB write, Supabase schema/DDL은 모두 보류한다.
  - `30일_실행계획.md` append는 동시 편집 충돌 방지를 위해 보류했다.
- 다음:
  1. game-console 실제 raw query 범위를 categoryId/source term 기반으로 넓혀 raw sample이 잡히는지 확인한다.
  2. non-AirPods headphone은 `rawFetchedUnique=42` 전부 out-of-scope인 이유를 query lane별로 분리한다.
  3. 여전히 raw sample이 부족하면 live-read/no-write acquisition rehearsal로 넘어가되, DB 저장과 후보 공개는 계속 금지한다.

## v2.221 - suite compiler helper layer

- 결정: `scripts/lib/report-packet-registry.ts`에 `PacketSuiteSpec`, `RegistryChainArtifacts`, `registryPacketSuites`, `compileChainArtifacts()`를 추가했다.
- 결정: 아래 chain 3종은 이제 category별 export를 직접 펼치지 않고 suite compiler 결과를 읽는다.
  - `scripts/report-parser-readiness-all.ts`
  - `scripts/report-parser-report-only-audit.ts`
  - `scripts/report-parser-manifest-audit.ts`
- 결정: helper/compiler 구조를 설명하는 별도 후보 리포트를 추가했다.
  - `scripts/report-parser-registry-compiler-candidate.ts`
  - `reports/parser-registry-compiler-candidate-latest.md`
- 결과:
  - `reports/parser-registry-pilot-status-latest.md`
  - `registry pilot files=37`
  - `fully_registry_backed=37`
  - `remaining_literals=0`
  - `reports/parser-registry-compiler-candidate-latest.md`
  - `groups=9`
  - `packets=108`
  - `helpers=4`
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion은 계속 보류한다.
  - full generic packet runner/spec compiler 구현은 아직 보류하고, 우선 suite helper/flatten 단계까지만 올린다.
  - `report-parser-category-evidence-ledger.ts`의 editorial narrative layer는 여전히 explicit하게 유지한다.
- 다음:
  1. suite helper를 더 일반화해 chain별 flattening 중복을 한 번 더 줄인다.
  2. `phase`/`tags` 기준 metadata 요약 report를 더 확장한다.
  3. 이후 새 packet 추가는 pilot 범위 family/category에서 registry-first + metadata-first + suite-compiler 규칙으로만 진행한다.

## v2.222 - editorial spec split and phase/tag summary

- 결정: `report-parser-category-evidence-ledger.ts`의 narrative fallback(`laneFallback`, `readinessMetricFallback`, `holdReasons`, `nextSafeReportOnlyTask`)을 별도 report-only spec으로 분리했다.
  - `scripts/lib/report-category-editorial-spec.ts`
- 결정: evidence registry와 editorial narrative를 섞지 않고, ledger에서만 두 레이어를 합치는 구조를 유지한다.
- 결정: registry metadata를 실제 backlog 요약에 쓰기 위해 phase/tag summary report를 추가했다.
  - `scripts/report-parser-registry-phase-tag-summary.ts`
  - `reports/parser-registry-phase-tag-summary-latest.md`
- 결과:
  - `reports/parser-category-evidence-ledger-latest.md`
  - `categories=10`
  - `evidence_reports=74`
  - `runtime_rows=0`
  - `reports/parser-registry-phase-tag-summary-latest.md`
  - `groups=9`
  - `packets=108`
  - `phases=2`
  - `tags=35`
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion은 계속 보류한다.
  - full generic packet runner/spec compiler는 여전히 보류하고, 지금은 suite helper + editorial spec + metadata summary 조합까지만 올린다.
  - `report-parser-category-evidence-ledger.ts`의 category/evidence mapping까지 완전 자동화하는 건 다음 단계로 미룬다.
- 다음:
  1. category/evidence mapping layer도 더 data-driven 하게 줄일 수 있는지 검토한다.
  2. phase/tag summary를 backlog signal report로 확장한다.
  3. 이후 새 packet 추가는 pilot 범위 family/category에서 registry-first + metadata-first + suite-compiler + editorial-spec 규칙으로만 진행한다.

## v2.223 - category evidence spec split and backlog signals

- 결정: `report-parser-category-evidence-ledger.ts`의 category/evidence mapping도 별도 spec로 분리했다.
  - `scripts/lib/report-category-evidence-spec.ts`
- 결정: ledger는 이제 3층을 조합한다.
  - evidence mapping spec
  - editorial fallback spec
  - readiness summary read
- 결정: registry metadata를 실제 우선순위 읽기에 쓰기 위해 backlog signal report를 추가했다.
  - `scripts/report-parser-registry-backlog-signals.ts`
  - `reports/parser-registry-backlog-signals-latest.md`
- 결과:
  - `reports/parser-category-evidence-ledger-latest.md`
  - `categories=10`
  - `evidence_reports=74`
  - `runtime_rows=0`
  - `reports/parser-registry-backlog-signals-latest.md`
  - `groups=9`
  - `positive_density=2`
  - `hold_lanes=3`
- 해석:
  - 현재 backlog 기준 최상단은 `smartwatch-wearables`
  - 그다음은 `earphone-airpods-galaxybuds`
  - `camera`, `speaker`는 hold-heavy boundary family라 한 단계 아래 구조 개선/검증 대상으로 유지한다
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion은 계속 보류한다.
  - backlog signal은 report-only prioritization aid일 뿐이며 next-work queue를 직접 mutate하지 않는다.
  - category/evidence/editorial 3층을 공용 helper로 더 줄이는 건 다음 단계로 미룬다.
- 다음:
  1. backlog signal report를 next-work queue와 연결할 수 있는지 검토한다.
  2. category/evidence/editorial spec 3층을 공통 helper로 더 줄일 수 있는지 본다.
  3. 이후 새 packet 추가는 pilot 범위 family/category에서 registry-first + metadata-first + suite-compiler + editorial-spec 규칙으로만 진행한다.

## v2.224 - backlog signals linked into next-work queue

- 결정: `parser-registry-backlog-signals-latest`를 `parser-next-work-queue`에 직접 연결했다.
- 결정: `parser-next-work-queue-latest.md`는 이제
  - `Guide Coverage Backlog Signals`
  - `Registry Backlog Signals`
  두 축을 함께 보여준다.
- 결과:
  - `reports/parser-next-work-queue-latest.md`
  - `rows=7`
  - `evidence_completed=7`
  - `reports/parser-registry-backlog-signals-latest.md`
  - `groups=9`
  - `positive_density=2`
  - `hold_lanes=3`
- 해석:
  - category queue와 family queue를 이제 같은 문서에서 함께 읽을 수 있다.
  - 현재 최상단 family 우선순위는 여전히 `smartwatch-wearables`, 그다음 `earphone-airpods-galaxybuds`다.
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion은 계속 보류한다.
  - backlog signal은 queue 문서에 보조 우선순위로만 연결하고, 실제 queue row를 자동 재정렬하거나 mutate하지는 않는다.
  - category/evidence/editorial 3층 공통 helper 정리는 다음 단계로 보류한다.
- 다음:
  1. category/evidence/editorial spec 3층을 공통 helper로 더 줄일 수 있는지 본다.
  2. 구조 개선을 여기서 잠깐 멈추고 high-priority family packet thickening으로 복귀할지 판단한다.
  3. 이후 새 packet 추가는 pilot 범위 family/category에서 registry-first + metadata-first + suite-compiler + editorial-spec 규칙으로만 진행한다.

## v2.221 - game-console no-write live search/detail sampling

- 결정: DB raw scope가 0건이었으므로, 운영 DB 적재 없이 번개장터 live search/read-only scope를 별도 측정했다.
- 결과:
  - `reports/game-console-live-search-scope-latest.md`
  - `liveFetchedUnique=455`, `candidatePositive=128`, `candidateRiskRows=0`, `uniqueCandidateKeys=11`
  - `reports/game-console-live-detail-sample-latest.md`
  - `sampledCandidateRows=40`, `stillCandidateRows=13`, `downgradedRows=27`, `downgradeRate=0.675`
- 해석:
  - game-console은 시장 데이터가 없는 게 아니라 현재 운영 raw DB lane이 비어 있다.
  - title-only search candidate는 상세 조회 후 67.5%가 sold/reserved/bundle/accessory/part/detail-risk로 내려가므로, DB acquisition이나 후보 공개 전에 detail gate가 필수다.
  - 이번에 발견한 downgrade는 runtime patch가 아니라 안전한 acquisition 설계 근거로만 사용한다.
- 보류:
  - game-console runtime parser 수정, candidate pool policy wiring, public promotion, DB write/acquisition 적용은 모두 보류한다.
  - `30일_실행계획.md` append는 동시 편집 충돌 방지를 위해 보류했다.
- 다음:
  1. game-console detail gate proposal을 report-only로 작성한다.
  2. search-only positive를 바로 raw acquisition하지 않고, live detail에서 `SELLING + no sold text + no bundle/accessory/parts`를 통과한 subset만 tiny no-write acquisition rehearsal 후보로 삼는다.
  3. detail gate 설계가 끝난 뒤에도 실제 DB write는 owner/main-agent 승인 전까지 금지한다.

## v2.222 - game-console detail gate tightened

- 결정: `reports/game-console-live-detail-sample-latest.md`의 first pass에서 추가 구성품/게임 bundle 오염이 still-candidate에 남아 있어, report-only detail gate 판정만 더 보수적으로 조정했다.
- 결과:
  - `reports/game-console-live-detail-sample-latest.md`
  - `sampledCandidateRows=40`, `stillCandidateRows=10`, `downgradedRows=30`, `downgradeRate=0.75`
  - `reports/game-console-detail-gate-proposal-latest.md`
  - `allowedNoWriteRehearsalRows=10`
- 해석:
  - game-console은 title-only 검색 후보를 바로 acquisition하면 위험하다.
  - 상세 기준으로 `SOLD_OUT/RESERVED`, 판매완료 텍스트, accessory/parts, 게임 타이틀/칩/SD/추가 조이콘 같은 가격 오염 요소를 먼저 gate해야 한다.
- 보류:
  - runtime parser patch, DB acquisition, candidate pool/public promotion은 계속 보류한다.
  - 이번 변경은 report-only gate proposal과 산출물 재생성에 한정한다.
- 다음:
  1. allowed no-write rehearsal rows 10개만 대상으로 tiny acquisition rehearsal 설계를 만든다.
  2. 실제 DB write는 owner/main-agent 승인 전까지 금지한다.

## v2.223 - game-console tiny no-write acquisition rehearsal plan

- 결정: detail gate를 통과한 game-console row 10개만 대상으로 tiny acquisition rehearsal plan을 report-only로 작성했다.
- 결과:
  - `reports/game-console-tiny-acquisition-rehearsal-plan-latest.md`
  - `allowedRows=10`, `comparableKeys=2`, `maxFutureWriteCap=10`
  - comparable keys: `game_console/nintendo_switch/oled/body_only`, `game_console/nintendo_switch/oled/full_set`
- 해석:
  - game-console 확장은 broad runtime 승격이 아니라 `Switch OLED body_only/full_set` 좁은 lane부터 검증하는 게 맞다.
  - future write-cap rehearsal이 있더라도 직전 fresh detail refetch, saleStatus SELLING 유지, comparableKey 유지, no bundle/accessory/parts/sold text 확인이 필요하다.
- 보류:
  - DB write/acquisition executor, candidate pool wiring, public promotion은 계속 보류한다.
- 다음:
  1. owner/main-agent 승인 전까지 이 plan은 구현 지시서가 아니라 검토 자료로만 둔다.
  2. 승인 시에도 `maxFutureWriteCap=10` 이하 tiny write-cap으로만 시작한다.

## v2.224 - headphone non-AirPods no-write detail gate and rehearsal plan

- 결정: 기존 subagent의 headphone model/spec evidence와 live search report를 바탕으로 non-AirPods headphone detail sample을 report-only로 수행했다.
- 결과:
  - `reports/headphone-non-airpods-live-detail-sample-latest.md`
  - `sampledCandidateRows=60`, `stillCandidateRows=41`, `downgradedRows=19`, `downgradeRate=0.317`, `stillCandidateSkus=12`
  - `reports/headphone-non-airpods-detail-gate-proposal-latest.md`
  - `reports/headphone-non-airpods-tiny-acquisition-rehearsal-plan-latest.md`
  - `allowedRows=41`, `skuLanes=12`, `strictLaneRows=14`, `maxFutureWriteCap=24`
- 해석:
  - non-AirPods headphone은 game-console보다 훨씬 acquisition 후보가 두껍다.
  - 다만 `sennheiser-accentum`, `bose-qc-ultra`, `sony-wh-ch720n`, `sennheiser-hd569`, `beats-solo4`는 earbuds/open-ear/accessory/counterfeit/edition 오염 가능성이 있어 strict lane으로 둔다.
  - search-only acquisition은 금지하고, fresh detail + exact SKU/comparableKey + SELLING + no accessory/parts/earbuds/counterfeit gate를 통과한 row만 no-write rehearsal 후보로 본다.
- 보류:
  - DB write/acquisition executor, candidate pool wiring, public promotion은 계속 보류한다.
  - `30일_실행계획.md` append는 동시 편집 충돌 방지를 위해 보류했다.
- 다음:
  1. strict lane은 더 보수적으로 두고, lower-risk Sony/Bose QC45 lane부터 no-write rehearsal subset을 검토한다.
  2. 실제 DB write는 owner/main-agent 승인 전까지 금지한다.

## v2.225 - headphone first-wave narrowed by subagent review

- 결정: non-AirPods headphone을 전체 lower-risk 7개 SKU로 바로 넓히지 않고, subagent review 결과를 반영해 첫 no-write rehearsal 후보를 `sony-wh-1000xm4`, `sony-wh-ch520`, `bose-qc45` 3개 SKU로 더 좁혔다.
- 결과:
  - `reports/headphone-lower-risk-acquisition-rehearsal-plan-latest.md`
  - `lowerRiskRows=27`, `lowerRiskSkus=7`
  - `firstWaveRows=12`, `firstWaveSkus=3`, `proposedNoWriteCap=12`
  - optional: `sony-wh-ult900n`
  - deferred: `sony-wh-1000xm5`, `sony-wh-1000xm3`, `sony-wh-1000xm6`
- 해석:
  - 첫 wave는 “많이 열기”보다 실제 시장 제목/상세에서 accessory/parts/earbuds/counterfeit 오염이 낮은 좁은 lane을 확인하는 쪽이 맞다.
  - `sony-wh-ult900n`은 ULT WEAR -> WH-ULT900N 매핑을 한 번 더 확인한 뒤 optional wave로만 둔다.
  - strict lane(`sennheiser-accentum`, `bose-qc-ultra`, `sony-wh-ch720n`, `sennheiser-hd569`, `beats-solo4`)은 계속 report-only 검토 대상으로 유지한다.
- 보류:
  - DB write/acquisition executor, candidate pool wiring, public promotion은 계속 보류한다.
  - `30일_실행계획.md` append는 동시 편집 충돌 방지를 위해 보류했다.
- 다음:
  1. headphone first-wave 12건을 실제 DB write 없이 fresh detail refetch + SELLING + same SKU/key + hard-negative gate로 재검증하는 rehearsal만 설계한다.
  2. 다른 main agent의 Supabase/runtime P0 작업과 충돌하지 않도록, runtime/cron/pack/schema 파일은 건드리지 않는다.

## v2.226 - headphone first-wave fresh detail rehearsal

- 결정: first-wave 12건을 실제 DB write 없이 번개장터 fresh detail로 다시 읽고, `SELLING + same SKU/key + no hard-negative` 기준을 통과하는지 확인했다.
- 결과:
  - `reports/headphone-first-wave-fresh-detail-rehearsal-latest.md`
  - `checkedRows=12`, `passingRows=7`, `failingRows=5`, `passRate=58.3%`
  - pass SKU: `sony-wh-1000xm4=3`, `sony-wh-ch520=4`
  - fail reason: `accessory_only_text=3`, `comparable_key_changed=3`, `damaged_or_parts_text=2`, `buying_or_trade_text=1`
- 해석:
  - headline/title 기준으로는 낮은 위험처럼 보였던 first-wave도 fresh detail을 붙이면 절반 가까이가 걸린다.
  - `bose-qc45`는 runtime comparable key가 `earphone|bose_quietcomfort_45` vs `earphone|bose_qc45`로 흔들려 첫 write-cap 후보에서 제외하는 게 맞다.
  - 실제 acquisition은 “search/title 후보 -> fresh detail 재확인 -> same key 유지”가 필수다.
- 보류:
  - DB write/acquisition executor, candidate pool wiring, public promotion은 계속 보류한다.
  - hard-negative regex를 runtime에 바로 반영하지 않고, report-only evidence로만 둔다.
- 다음:
  1. headphone 첫 tiny acquisition 후보는 `sony-wh-1000xm4`, `sony-wh-ch520`의 passing rows 7건 이하로만 좁힌다.
  2. `bose-qc45`는 comparable key normalization이 정리될 때까지 보류한다.
  3. 다음 report-only 작업은 passing rows 7건 기준의 “strict write-cap contract” 문서화다.

## v2.227 - headphone strict write-cap contract prepared

- 결정: fresh detail 재검증을 통과한 row만 대상으로 future write-cap 계약서를 report-only로 작성했다.
- 결과:
  - `reports/headphone-first-wave-strict-write-cap-contract-latest.md`
  - `allowedRows=7`, `allowedSkus=2`, `maxFutureWriteCap=7`
  - allowed SKU: `sony-wh-1000xm4`, `sony-wh-ch520`
  - deferred SKU: `bose-qc45`, `sony-wh-ult900n`, `sony-wh-1000xm5`, `sony-wh-1000xm3`, `sony-wh-1000xm6`, `sony-wh-ch720n`, `bose-qc-ultra`, `sennheiser-accentum`, `sennheiser-hd569`, `beats-solo4`
- 해석:
  - non-AirPods headphone은 실제 데이터가 충분히 있지만, 첫 적용은 7건 이하의 매우 좁은 no-public write-cap으로만 고려해야 한다.
  - 이 계약은 실행 지시가 아니라 owner/main-agent review packet이며, 현재 다른 main agent의 Supabase/runtime P0 작업이 끝나기 전에는 실행하지 않는다.
- 보류:
  - DB write/acquisition executor, candidate pool wiring, public promotion은 계속 보류한다.
  - runtime key normalization(`bose-qc45`)은 별도 parser/runtime 작업으로 분리한다.
- 다음:
  1. 다른 main agent의 P0 안정화가 끝나면 이 contract를 owner review에 올린다.
  2. 그 전까지는 다음 카테고리 no-write live/detail 검증만 진행한다.

## v2.228 - category live-read rerun and monitor first-lane contract

- 결정: 카메라/모니터/스피커/로봇청소기 live-read runner를 재실행해, Supabase write 없이 다음 category 후보 우선순위를 다시 봤다.
- 결과:
  - `reports/category-no-write-live-read-observation-latest.md`
  - total: `searchRowsRead=58`, `detailRowsRead=32`, `freshLiveCandidates=11`, `manualReviewRows=6`, `holdRows=15`
  - camera: fresh/manual/hold `2/3/3`
  - monitor: fresh/manual/hold `6/0/2`
  - speaker: fresh/manual/hold `3/2/3`
  - robot vacuum: fresh/manual/hold `0/1/7`
  - `reports/monitor-selected-exact-model-strict-contract-latest.md`
  - monitor first lane: `benq-zowie-xl2540k`, `strictAllowedRows=6`, `maxFutureWriteCap=5`
- 해석:
  - 바로 다음 확장 후보는 모니터 `XL2540K` exact-model lane이 가장 두껍다.
  - `AW2525HM`은 active row가 1건뿐이라 optional later로 두고, SOLD_OUT row는 제외한다.
  - subagent review 결과 `모니터 암까지 드립니다`는 accessory-only가 아니라 bundled extra라 false hold로 복구했다.
  - 로봇청소기는 accessory/소모품/예약/사기 경고가 많아 지금은 acquisition 후보가 아니라 룰 보강/검색어 재조정 대상이다.
- 보류:
  - monitor runtime parser patch, DB acquisition, candidate pool wiring, public promotion은 계속 보류한다.
  - `30일_실행계획.md` append는 동시 편집 충돌 방지를 위해 보류했다.
- 다음:
  1. monitor `XL2540K` strict contract는 owner review packet으로만 둔다.
  2. 이어서 speaker selected subset을 fresh detail 기준으로 더 좁혀, 모니터 다음 후보인지 확인한다.

## v2.229 - speaker selected subset strict contract

- 결정: category live-read 결과 중 speaker selected subset을 `JBL Flip 6` 첫 lane으로 좁혀 strict write-cap contract를 report-only로 작성했다.
- 결과:
  - `reports/speaker-selected-subset-strict-contract-latest.md`
  - `strictAllowedRows=3`, `maxFutureWriteCap=3`
  - allowed SKU/model: `jbl-flip-6`
  - excluded examples: rental listing, speaker+other speaker bundle, case-only accessory, model-only title without speaker context
- 해석:
  - speaker는 monitor보다 얇지만, `JBL Flip 6`은 internal acquisition 후보로 검토 가능한 최소 두께가 있다.
  - accessory-only는 “케이스/파우치/케이블”이 단독 제목 문맥이면 hold하고, fullbox/included accessory는 별도 문맥 검토가 필요하다.
- 보류:
  - speaker runtime parser patch, DB acquisition, candidate pool wiring, public promotion은 계속 보류한다.
  - model-only title(`JBL FLIP6`)을 자동 통과시키는 건 description/context 보강 전까지 보류한다.
- 다음:
  1. 다음 category pass는 camera body-only exact model을 strict contract로 좁힌다.
  2. robot vacuum은 fresh 후보가 없으므로 현 단계에서는 write-cap 후보가 아니라 rule/query refinement 대상으로만 둔다.

## v2.230 - camera body-only strict contract

- 결정: camera body-only exact model lane을 `Sony A7M3 body-only`로 좁혀 strict write-cap contract를 report-only로 작성했다.
- 결과:
  - `reports/camera-body-only-strict-contract-latest.md`
  - `strictAllowedRows=2`, `maxFutureWriteCap=2`
  - allowed model: `sony-a7m3`
  - hold examples: A7M3 reserved rows, A7M3 buying row, A7M2/A7M4 mismatch, body-cap accessory
- 해석:
  - camera는 가능성은 있지만 현재 live-read 표본이 얇다.
  - A7M3 body-only는 내부 관측 후보로는 의미가 있으나, write-cap을 검토하려면 최소 한 번 더 live-read wave를 쌓는 게 맞다.
- 보류:
  - camera runtime parser patch, DB acquisition, candidate pool wiring, public promotion은 계속 보류한다.
  - lens/body/package taxonomy runtime 적용은 별도 owner review 전까지 보류한다.
- 다음:
  1. 우선순위는 monitor XL2540K > speaker JBL Flip 6 > camera A7M3 body-only 순서로 둔다.
  2. 다음 작업은 이 세 strict contract를 하나의 acquisition readiness ranking으로 묶어 owner review packet을 만든다.

## v2.231 - next acquisition readiness ranking

- 결정: headphone/monitor/speaker/camera strict contract를 하나의 report-only ranking으로 묶어, 다음 내부 acquisition 후보 우선순위를 정리했다.
- 결과:
  - `reports/next-acquisition-readiness-ranking-latest.md`
  - rank 1: `monitor_discovered / benq-zowie-xl2540k`, rows 6, cap 5
  - rank 2: `headphone_discovered / sony-wh-1000xm4, sony-wh-ch520`, rows 7, cap 7
  - rank 3: `speaker_audio_discovered / jbl-flip-6`, rows 3, cap 3
  - rank 4: `camera_discovered / sony-a7m3`, rows 2, cap 2
- 해석:
  - 다음 owner review는 monitor XL2540K부터 보는 게 가장 효율적이다.
  - headphone은 row 수는 충분하지만 fresh detail 탈락률이 컸으므로 same SKU/key gate가 더 중요하다.
  - speaker/camera는 가능하지만 아직 thin lane이므로 추가 live-read wave가 있으면 더 안전하다.
- 보류:
  - 위 ranking은 실행 명령이 아니며, DB write/acquisition executor, candidate pool wiring, public promotion은 계속 보류한다.
  - 다른 main agent의 P0 Supabase/runtime 안정화가 끝나기 전에는 실행하지 않는다.
- 다음:
  1. 다른 main agent P0 안정화 완료 후 monitor XL2540K contract를 owner review에 올린다.
  2. 그 전까지는 report-only 검증과 spec/model guide evidence 보강만 진행한다.

## v2.232 - monitor XL2540K owner review packet

- 결정: acquisition readiness ranking 1순위인 monitor `benq-zowie-xl2540k`를 owner review packet으로 분리했다.
- 결과:
  - `reports/monitor-xl2540k-owner-review-packet-latest.md`
  - `strictAllowedRows=6`, `maxFutureWriteCap=5`, `falseHoldRecoveredRows=1`
  - `AW2525HM`은 active singleton이라 optional later로 유지
- 해석:
  - monitor XL2540K는 현 시점에서 가장 준비도가 높은 다음 내부 acquisition 후보지만, 이 packet은 실행 명령이 아니다.
  - future execution 전 조건은 `other_main_agent_p0_done`, fresh detail refetch, same model code, SELLING, no public promotion, max write cap 5다.
  - accessory term은 accessory-only title일 때만 hold해야 하고, `모니터 암까지 드립니다` 같은 bundled extra는 false hold로 회수 가능하다.
- 보류:
  - public candidate pack promotion, candidate pool wiring, runtime catalog apply, DB acquisition executor, schema/migration은 전부 보류한다.
  - `30일_실행계획.md` append는 동시 편집 충돌 방지를 위해 보류했다.
- 다음:
  1. 다른 main agent P0 안정화 완료 전까지 이 packet은 review-only로 둔다.
  2. 그 사이 추가 작업은 report-only second live-read wave 또는 spec/model guide evidence 보강으로 제한한다.

## v2.233 - second live-read wave consistency check

- 결정: category no-write live-read runner를 한 번 더 실행해 monitor/speaker/camera strict contract가 같은 숫자로 재현되는지 확인했다.
- 결과:
  - `reports/category-no-write-live-read-observation-latest.md`
  - total: `searchRowsRead=58`, `detailRowsRead=32`, `freshLiveCandidates=11`, `manualReviewRows=6`, `holdRows=15`
  - 재생성 후:
    - monitor XL2540K: `strictAllowedRows=6`, `maxFutureWriteCap=5`
    - speaker JBL Flip 6: `strictAllowedRows=3`, `maxFutureWriteCap=3`
    - camera A7M3 body-only: `strictAllowedRows=2`, `maxFutureWriteCap=2`
    - ranking top: `benq-zowie-xl2540k`
- 해석:
  - low-volume second wave에서도 ranking이 흔들리지 않았다.
  - monitor XL2540K는 여전히 다음 owner review 1순위다.
- 보류:
  - 이 재현성 확인은 report-only이며 DB write/acquisition executor/public promotion은 계속 보류한다.
  - 다른 main agent P0 작업 완료 전에는 실행으로 넘어가지 않는다.
- 다음:
  1. owner review 전까지는 spec/model guide evidence 보강 또는 추가 no-write live-read만 수행한다.
  2. runtime/Supabase 파일은 다른 main agent 작업과 충돌하지 않게 계속 건드리지 않는다.

## v2.234 - monitor owner packet tightened after subagent review

- 결정: subagent가 지적한 monitor XL2540K owner packet의 누락을 반영했다.
- 반영:
  - owner packet sourceReports에 `monitor-model-code-spec-evidence-packet-latest.json`, `monitor-selected-exact-model-source-confidence-latest.json` 추가
  - owner packet에 XL2540K official source/spec row 직접 추가
  - strict contract의 전체 gate를 owner packet execution prerequisites에 반영
  - optional `AW2525HM` singleton과 true excluded `SOLD_OUT AW2525HM`을 분리
- 결과:
  - `reports/monitor-xl2540k-owner-review-packet-latest.md`
  - `reports/monitor-xl2540k-execution-readiness-checklist-latest.md`
  - readiness checklist: `passChecks=8`, `warningChecks=0`, `failedChecks=0`
- 해석:
  - monitor XL2540K packet은 이제 report-only owner review 자료로 충분히 닫힌 형태다.
  - 하지만 실행은 여전히 보류다. P0 안정화 완료와 owner 승인 없이는 DB acquisition executor를 만들지 않는다.
- 보류:
  - public promotion, candidate pool wiring, runtime catalog apply, DB acquisition executor, schema/migration 전부 보류한다.
- 다음:
  1. P0 안정화가 끝나기 전까지는 이 packet을 dormant 상태로 둔다.
  2. 이후 작업은 report-only evidence 보강 또는 다른 category live-read 검증으로 제한한다.

## v2.228 - category status context helper

- 결정: `category + evidence + editorial + readiness` 4층 조합을 공용 helper로 뽑았다.
  - `scripts/lib/report-category-status-context.ts`
- 결정: `report-parser-category-evidence-ledger.ts`는 이제 직접 readiness json을 풀지 않고 공용 helper가 만들어주는 context를 읽는다.
- 결정: `report-parser-next-work-queue.ts`도 readiness row 로딩을 같은 helper 경로로 통일했다. 아직 full context 소비까지는 올리지 않고, 우선 readiness read 접착제부터 공통화했다.
- 결정: helper 배선이 실제로 전 category에서 맞물리는지 보는 구조 검증 report를 추가했다.
  - `scripts/report-parser-category-context-status.ts`
  - `reports/parser-category-context-status-latest.md`
- 결과:
  - `reports/parser-category-evidence-ledger-latest.md`
  - `categories=10`
  - `evidence_reports=74`
  - `runtime_rows=0`
  - `reports/parser-next-work-queue-latest.md`
  - `rows=7`
  - `evidence_completed=7`
  - `reports/parser-category-context-status-latest.md`
  - `categories=10`
  - `with_readiness=10`
  - `evidence_reports=74`
- 해석:
  - ledger/queue에서 반복되던 readiness read 접착제가 공통 helper 뒤로 들어갔다.
  - category/evidence/editorial/readiness 4층이 실제로 전 category에서 모두 연결되는지도 별도 상태 report로 확인 가능해졌다.
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion은 계속 보류한다.
  - `report-parser-next-work-queue.ts`가 category context helper를 full 소비하도록 더 크게 리팩터하는 건 아직 보류한다.
  - generic packet runner/spec compiler 본체 구현은 계속 보류한다.
- 다음:
  1. `category/evidence/editorial/readiness` 4층을 queue/ledger 외 다른 report script에도 공통 helper로 확장할지 본다.
  2. 구조 개선을 한 턴 더 밀지, 아니면 backlog signal 최상단인 `smartwatch-wearables` thickening으로 복귀할지 판단한다.
  3. 이후 새 packet 추가는 registry-first + metadata-first + suite-compiler + editorial-spec + category-context-helper 규칙으로만 진행한다.

## v2.229 - suite status/coverage made data-driven and manifest generation restored

- 결정: `report-parser-suite-status.ts`가 hold index만 읽어서 `monitor_discovered` 같은 hold lane을 누락하던 구조를, `category + evidence + editorial + readiness` context를 읽는 구조로 교체했다.
- 결정: `report-parser-suite-coverage.ts`의 `candidate/hold` 고정 기대치는 버리고, 추적 대상 category union coverage를 검증하도록 바꿨다.
- 결정: 누락되어 있던 `parser-report-manifest` 생성 단계를 별도 스크립트로 복구했다.
  - `scripts/report-parser-report-manifest.ts`
- 결정: `report-parser-readiness-all.ts` 순서를 바꿔 `suite-status -> report-manifest -> manifest-audit -> suite-coverage`로 재배치했다.
- 반영:
  - `scripts/report-parser-suite-status.ts`
  - `scripts/report-parser-suite-coverage.ts`
  - `scripts/report-parser-readiness-all.ts`
  - `scripts/report-parser-report-only-audit.ts`
  - `package.json`
- 결과:
  - `reports/parser-manifest-audit-latest.json`: `status=ok`, `checked=126`, `missing=0`
  - `reports/parser-suite-coverage-latest.json`: `status=ok`
  - `reports/parser-readiness-all-run-latest.json`: `steps=143`
  - `reports/parser-report-only-audit-latest.json`: `forbiddenDirtyCount=9`
- 해석:
  - 이번 문제는 smartwatch packet 3개 자체보다, manifest 생성기가 파이프라인에서 빠져 있고 `suite-coverage`가 lane 이동을 허용하지 않는 구조 결합 때문이었다.
  - 지금은 lane이 candidate에서 hold로 이동해도 `suite-status/coverage`가 안 깨지고, 새 packet 추가 뒤 manifest도 자동으로 따라간다.
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion은 계속 보류한다.
  - `forbiddenDirtyCount=9`는 workspace 상태 기록일 뿐이며, 금지 경로를 우리가 건드린 것으로 해석하지 않는다.
  - generic packet runner/spec compiler 본체 구현은 계속 보류한다.
- 다음:
  1. 구조 복구가 끝났으니 다시 `smartwatch-wearables` thickening으로 복귀한다.
  2. `Apple Watch Series 9` / `Galaxy Watch 7/8 44mm` narrow lane에서 clean adjacent or personal-used density를 더 파고든다.
  3. 이후 새 packet 추가는 반드시 `registry -> report-manifest -> manifest-audit -> suite-coverage` 체인을 통과시키는 규칙으로만 진행한다.

## v2.235 - JBL Flip 6 source backfill and owner packet closure

- 결정: speaker JBL Flip 6는 live-read strict contract만으로 바로 실행하지 않고, official source/spec backfill을 먼저 report-only로 보강했다.
- 생성:
  - `scripts/report-speaker-jbl-flip6-source-backfill.ts`
  - `reports/speaker-jbl-flip6-source-backfill-latest.md`
  - `reports/speaker-jbl-flip6-source-backfill-latest.json`
  - `scripts/report-speaker-jbl-flip6-owner-review-packet.ts`
  - `reports/speaker-jbl-flip6-owner-review-packet-latest.md`
  - `reports/speaker-jbl-flip6-owner-review-packet-latest.json`
  - `scripts/report-speaker-jbl-flip6-execution-readiness-checklist.ts`
  - `reports/speaker-jbl-flip6-execution-readiness-checklist-latest.md`
  - `reports/speaker-jbl-flip6-execution-readiness-checklist-latest.json`
- 결과:
  - official source/spec evidence rows: `2`
  - owner packet: `speaker_jbl_flip6_owner_review_packet_ready_report_only_no_execution`
  - execution checklist: `passChecks=7`, `warningChecks=1`, `failedChecks=0`
  - warning: 기존 `speaker-portable-exact-model-contract`가 아직 `jbl-flip-6`를 positive allowed model로 포함하지 않는다.
- 해석:
  - JBL Flip 6는 3개 fresh live row + official source evidence까지는 갖췄다.
  - 다만 row가 얇고 기존 portable exact-model contract와 완전히 합쳐지지 않았으므로, 즉시 runtime/DB execution이 아니라 owner review용으로만 닫는다.
- 보류:
  - public promotion, candidate pool wiring, runtime catalog apply, DB acquisition executor, schema/migration은 전부 보류한다.
  - `30일_실행계획.md` append는 다른 main-agent 동시 편집 충돌 방지를 위해 계속 보류한다.
- 다음:
  1. speaker 쪽은 `jbl-flip-6`를 portable exact-model contract positive lane에 통합할지 별도 report-only patch proposal로 검토한다.
  2. runtime/Supabase 안정화가 끝나기 전까지는 monitor/headphone/speaker packet을 실행하지 않고 dormant 상태로 둔다.
  3. 다음 report-only 후보는 `camera A7M3 owner packet` 또는 `speaker exact-model contract merge proposal` 중 하나로 진행한다.

## v2.236 - Sony A7M3 source backfill and thin owner packet

- 결정: camera Sony A7M3는 official source evidence를 보강하되, live row가 2건뿐이라 실행 준비는 실패 상태로 유지했다.
- 생성:
  - `scripts/report-camera-sony-a7m3-source-backfill.ts`
  - `reports/camera-sony-a7m3-source-backfill-latest.md`
  - `reports/camera-sony-a7m3-source-backfill-latest.json`
  - `scripts/report-camera-sony-a7m3-owner-review-packet.ts`
  - `reports/camera-sony-a7m3-owner-review-packet-latest.md`
  - `reports/camera-sony-a7m3-owner-review-packet-latest.json`
  - `scripts/report-camera-sony-a7m3-execution-readiness-checklist.ts`
  - `reports/camera-sony-a7m3-execution-readiness-checklist-latest.md`
  - `reports/camera-sony-a7m3-execution-readiness-checklist-latest.json`
- 결과:
  - official source/spec evidence rows: `2`
  - owner packet: `camera_sony_a7m3_owner_review_packet_ready_but_second_wave_required`
  - execution checklist: `passChecks=6`, `warningChecks=1`, `failedChecks=1`
  - fail reason: second no-write live-read wave required; 현재 clean body-only row가 `2`건뿐이다.
- 해석:
  - A7M3는 모델/공식 근거는 충분하지만, 고가 카메라 false-positive 리스크 때문에 row 2건으로는 실행하지 않는다.
  - body-only/no-lens 문맥, full-box/package ambiguity, buying/reserved hold를 계속 강하게 유지한다.
- 보류:
  - public promotion, candidate pool wiring, runtime catalog apply, DB acquisition executor, schema/migration은 전부 보류한다.
  - `30일_실행계획.md` append는 다른 main-agent 동시 편집 충돌 방지를 위해 계속 보류한다.
- 다음:
  1. camera는 second no-write live-read wave로 clean A7M3 body-only row가 최소 4건 이상 되는지 확인한다.
  2. 당장 실행 후보는 여전히 monitor XL2540K > headphone Sony XM4/CH520 > speaker Flip 6 순서다.
  3. 다음 report-only 작업은 ranking board를 갱신해 source/checklist 상태까지 반영하거나, speaker Flip 6 exact contract merge proposal을 작성한다.

## v2.237 - acquisition owner readiness board

- 결정: monitor/headphone/speaker/camera의 owner review 상태를 한 장으로 모으는 acquisition board를 생성했다.
- 생성:
  - `scripts/report-acquisition-owner-readiness-board.ts`
  - `reports/acquisition-owner-readiness-board-latest.md`
  - `reports/acquisition-owner-readiness-board-latest.json`
- 결과:
  - board entries: `4`
  - top target: `benq-zowie-xl2540k`
  - status order:
    1. `monitor_discovered / benq-zowie-xl2540k`: `owner_review_ready`
    2. `headphone_discovered / sony-wh-1000xm4, sony-wh-ch520`: `owner_review_ready_needs_checklist`
    3. `speaker_audio_discovered / jbl-flip-6`: `owner_review_with_warning`
    4. `camera_discovered / sony-a7m3`: `blocked`
- 해석:
  - 다음 실행 후보 자체는 monitor가 가장 깨끗하다.
  - headphone은 점수와 row는 좋지만 monitor/speaker/camera와 같은 owner packet/checklist layer가 아직 없어 이걸 먼저 채워야 한다.
  - speaker는 warning만 남았고, camera는 second live wave 전까지 blocked다.
- 보류:
  - public promotion, candidate pool wiring, runtime catalog apply, DB acquisition executor, schema/migration은 전부 보류한다.
  - `30일_실행계획.md` append는 다른 main-agent 동시 편집 충돌 방지를 위해 계속 보류한다.
- 다음:
  1. headphone Sony XM4/CH520 owner packet/checklist를 생성해 board의 `needs_checklist`를 해소한다.
  2. 그 뒤 P0 안정화 상태에 따라 monitor/headphone 중 tiny internal-only no-public executor를 owner 승인 대상으로 비교한다.

## v2.238 - headphone Sony XM4/CH520 owner packet and checklist

- 결정: headphone Sony first wave는 source backfill, owner packet, execution checklist까지 채워 monitor와 같은 최종 검수층으로 올렸다.
- 생성:
  - `scripts/report-headphone-sony-first-wave-source-backfill.ts`
  - `reports/headphone-sony-first-wave-source-backfill-latest.md`
  - `reports/headphone-sony-first-wave-source-backfill-latest.json`
  - `scripts/report-headphone-sony-first-wave-owner-review-packet.ts`
  - `reports/headphone-sony-first-wave-owner-review-packet-latest.md`
  - `reports/headphone-sony-first-wave-owner-review-packet-latest.json`
  - `scripts/report-headphone-sony-first-wave-execution-readiness-checklist.ts`
  - `reports/headphone-sony-first-wave-execution-readiness-checklist-latest.md`
  - `reports/headphone-sony-first-wave-execution-readiness-checklist-latest.json`
- 갱신:
  - `scripts/report-acquisition-owner-readiness-board.ts`
  - `reports/acquisition-owner-readiness-board-latest.md`
  - `reports/acquisition-owner-readiness-board-latest.json`
- 결과:
  - headphone source rows: `2`
  - owner packet: `headphone_sony_first_wave_owner_review_packet_ready_report_only_no_execution`
  - execution checklist: `passChecks=8`, `failedChecks=0`
  - board에서 headphone은 더 이상 `needs_checklist`가 아니라 `owner_review_ready`로 반영된다.
- 해석:
  - monitor XL2540K와 headphone Sony XM4/CH520은 둘 다 checklist-ready다.
  - headphone은 row/cap이 더 크지만, fresh detail rejected 5/12 이력이 있어 same SKU/key 재확인이 특히 중요하다.
- 보류:
  - public promotion, candidate pool wiring, runtime catalog apply, DB acquisition executor, schema/migration은 전부 보류한다.
  - `30일_실행계획.md` append는 다른 main-agent 동시 편집 충돌 방지를 위해 계속 보류한다.
- 다음:
  1. P0 안정화가 끝난 뒤 owner 승인 대상으로 monitor XL2540K와 headphone Sony XM4/CH520를 비교한다.
  2. 현재 report-only 순위는 monitor/headphone ready, speaker warning, camera blocked다.
  3. 다음 안전 작업은 speaker Flip 6 exact contract warning 해소 또는 camera second no-write live-read wave다.

## v2.239 - speaker JBL Flip 6 exact contract merge proposal

- 결정: speaker Flip 6 checklist warning의 원인을 runtime이 아니라 기존 portable exact-model contract의 positive lane 누락으로 분리했다.
- 생성:
  - `scripts/report-speaker-jbl-flip6-exact-contract-merge-proposal.ts`
  - `reports/speaker-jbl-flip6-exact-contract-merge-proposal-latest.md`
  - `reports/speaker-jbl-flip6-exact-contract-merge-proposal-latest.json`
- 결과:
  - proposedPositiveRows: `3`
  - proposedNegativeRows: `5`
  - sourceRows: `2`
  - conclusion: `speaker_jbl_flip6_exact_contract_merge_proposal_ready_report_only`
- 해석:
  - Flip 6 warning은 “근거 없음”이 아니라 “기존 portable exact-model contract에 아직 편입되지 않음”이다.
  - 별도 report-only patch로 contract를 갱신하면 speaker checklist warning을 해소할 수 있다.
- 보류:
  - 이번 작업은 merge proposal만 만들었고 기존 contract 파일은 변경하지 않았다.
  - public promotion, candidate pool wiring, runtime catalog apply, DB acquisition executor, schema/migration은 전부 보류한다.
  - `30일_실행계획.md` append는 다른 main-agent 동시 편집 충돌 방지를 위해 계속 보류한다.
- 다음:
  1. owner가 원하면 `speaker-portable-exact-model-contract`에 Flip 6 proposal을 report-only로 반영하고 checklist를 재생성한다.
  2. 그렇지 않으면 camera second no-write live-read wave로 A7M3 blocked 상태를 줄인다.

## v2.240 - subagent audit fixes for owner readiness artifacts

- 결정: subagent audit에서 지적한 정합성 문제를 즉시 반영했다.
- 수정:
  - `speaker-selected-subset-strict-contract`의 required gate를 실제 cap과 맞춰 `max_write_cap_3_rows`로 수정
  - `acquisition-owner-readiness-board`의 stale nextStep 문구를 수정해 headphone checklist 완료 상태를 반영
- 재생성:
  - `reports/speaker-selected-subset-strict-contract-latest.md/json`
  - `reports/speaker-jbl-flip6-owner-review-packet-latest.md/json`
  - `reports/speaker-jbl-flip6-execution-readiness-checklist-latest.md/json`
  - `reports/speaker-jbl-flip6-exact-contract-merge-proposal-latest.md/json`
  - `reports/acquisition-owner-readiness-board-latest.md/json`
- 결과:
  - speaker strict cap: `maxFutureWriteCap=3`
  - speaker checklist: `passChecks=7`, `warningChecks=1`, `failedChecks=0`
  - board top target: `benq-zowie-xl2540k`
- 해석:
  - cap mismatch는 해소됐다.
  - speaker의 남은 warning은 cap 문제가 아니라 exact contract merge 미적용 문제다.
- 보류:
  - 기존 speaker exact contract 본체를 아직 수정하지 않았다. merge proposal만 존재한다.
  - public promotion, candidate pool wiring, runtime catalog apply, DB acquisition executor, schema/migration은 전부 보류한다.
  - `30일_실행계획.md` append는 다른 main-agent 동시 편집 충돌 방지를 위해 계속 보류한다.
- 다음:
  1. P0 안정화 전에는 실행하지 않는다.
  2. report-only로 더 진행한다면 camera second live-read wave 또는 speaker exact contract merge 적용안 중 하나를 선택한다.


## v2.230 - phones discovered kept as next parser-heavy wave, not current switch

- 결정: `Galaxy S23 / iPhone 13 / Galaxy S25` 계열 관찰은 중요하지만, 현재 wave를 중간 전환하지 않고 다음 parser-heavy wave 1순위 후보로 올린다.
- 관찰:
  - `changed` 비율이 높고
  - row volume / turnover도 충분하며
  - readiness가 `internal_only`에 묶인 핵심 이유는 시장성 부족이 아니라 `trusted comparable_key` 부족으로 보인다.
- 해석:
  - 휴대폰 카테고리는 데이터 부족 문제가 아니라 옵션 파서/정규화 신뢰도 문제다.
  - 즉 다음 wave에서 value가 큰 축은 `세대 / 용량 / 색상 / 자급제 / 통신사 / dual-SIM / eSIM` 같은 option axis를 안전하게 분리하는 comparable trust 강화다.
- 보류:
  - 현재 wave에서는 runtime parser wiring, candidate pool policy wiring, public promotion으로 넘어가지 않는다.
  - 현재 진행 중인 `smartwatch-wearables` thickening과 report-only orchestration 구조 정리를 끊지 않는다.
- 다음:
  1. 현재 wave는 `Apple Watch Series 9`, `Galaxy Watch 7/8 44mm` narrow lane thickening을 계속한다.
  2. 다음 parser-heavy wave 시작 시 `phones_discovered`를 1순위 후보로 올려 option parser/comparable_key trust 보강 설계를 연다.
  3. 그때는 `row volume/turnover는 충분하고 trusted comparable_key가 부족하다`는 전제를 기준으로 휴대폰 lane을 본다.

## v2.241 - speaker Flip 6 exact contract backfill applied report-only

- 결정: speaker JBL Flip 6 warning은 runtime 문제가 아니라 report-only exact contract가 최신 source/detail evidence를 읽지 못하는 상태로 판단하고, `speaker-portable-exact-model-contract` generator에 Flip 6 backfill을 반영했다.
- 수정/재생성:
  - `scripts/report-speaker-portable-exact-model-contract.ts`
  - `reports/speaker-portable-exact-model-contract-latest.md/json`
  - `reports/speaker-jbl-flip6-owner-review-packet-latest.md/json`
  - `reports/speaker-jbl-flip6-execution-readiness-checklist-latest.md/json`
  - `reports/speaker-jbl-flip6-exact-contract-merge-proposal-latest.md/json`
  - `reports/acquisition-owner-readiness-board-latest.md/json`
- 결과:
  - speaker exact contract: `candidatePositiveContractRows=8`, `flip6CandidateRows=3`, `flip6HoldRows=5`
  - speaker checklist: `passChecks=8`, `warningChecks=0`, `failedChecks=0`
  - acquisition board: monitor/headphone/speaker가 `owner_review_ready`, camera만 `blocked`
- 해석:
  - Flip 6은 이제 source-backed strict report-only 계약 기준으로 owner review ready다.
  - 실제 runtime/catalog/candidate pool/public promotion/DB write는 전혀 수행하지 않았다.
- 보류:
  - P0 Supabase/runtime 안정화 전에는 실행 executor를 만들지 않는다.
  - `30일_실행계획.md` append는 다른 main-agent 동시 편집 충돌 방지를 위해 계속 보류한다.
- 다음:
  1. camera A7M3 blocked 해소를 위해 second no-write live-read wave를 실행한다.
  2. ready 3개는 owner 승인 전까지 dormant 상태로 유지한다.


## v2.231 - next smartwatch packet wave: Series10 46mm positive, Watch7 anchor stays blocked

- 결정: subagent 추천을 받아 다음 smartwatch wave로 `Apple Watch Series10 46mm battery90+ clean personal-used split`와 `Galaxy Watch7 44mm bluetooth explicit no-conflict non-merchant personal-used thickening` 두 packet을 추가했다.
- 생성:
  - `scripts/report-smartwatch-applewatch-series10-46mm-battery90plus-clean-personal-used-split.ts`
  - `scripts/report-smartwatch-galaxywatch-watch7-44mm-bluetooth-explicit-noconflict-nonmerchant-personal-used-thickening.ts`
- 결과:
  - Series10 46mm battery90+
    - `baseRows=3`
    - `cleanPersonalUsedRows=2`
    - `bundleRows=1`
    - `cellularRows=1`
  - Galaxy Watch7 44mm bluetooth explicit no-conflict non-merchant personal-used
    - `baseRows=2`
    - `nonMerchantRows=1`
    - `personalUsedRows=1`
    - `cleanRows=0`
- 해석:
  - `Apple Watch Series10 46mm battery90+`는 이전 titanium lane보다 더 직접적인 clean personal-used 가능성을 보여준다. 현재는 tiny하지만 `Series9`와 함께 다음 thickening 비교 lane으로 유지할 가치가 있다.
  - `Galaxy Watch7 44mm`는 더 정직하게 좁히면 anchor lane이 바로 비어 버린다. 즉 현재 Galaxy Watch family는 여전히 guardrail/pressure 해석이 우선이며, broad positive 서사는 금지한다.
- 반영:
  - registry / readiness / manifest 체인에 두 packet을 연결했다.
  - `parser-report-manifest-latest`: `rows=128`, `missing=0`
  - `parser-manifest-audit-latest`: `status=ok`, `checked=128`, `missing=0`
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion은 계속 보류한다.
  - Galaxy Watch는 clean anchor density가 생기기 전까지 promotion candidate가 아니라 negative proof/guardrail family로 유지한다.
- 다음:
  1. Apple Watch는 `Series10 46mm battery90+`와 `Series9 45mm battery90+`를 personal-used cleanliness / connectivity baggage 기준으로 직접 비교하는 다음 packet을 연다.
  2. Galaxy Watch는 `Watch7/8`에서 clean lane을 더 두껍게 만들기보다, 어떤 baggage가 clean lane을 막는지 negative-proof packet을 더 우선한다.


## v2.232 - Apple Watch Series10 46mm now beats Series9 on clean row count

- 결정: `Apple Watch Series10 46mm battery90+ clean personal-used split`와 이를 `Series9 45mm battery90+`와 직접 비교하는 packet까지 추가했다.
- 생성:
  - `scripts/report-smartwatch-applewatch-series10-46mm-battery90plus-clean-personal-used-split.ts`
  - `scripts/report-smartwatch-applewatch-series10-46mm-vs-series9-45mm-battery90plus-cleanliness.ts`
  - `scripts/report-smartwatch-galaxywatch-watch7-44mm-bluetooth-explicit-noconflict-nonmerchant-personal-used-thickening.ts`
- 결과:
  - Series10 46mm battery90+ split
    - `baseRows=3`
    - `cleanPersonalUsedRows=2`
    - `bundleRows=1`
    - `cellularRows=1`
  - Series10 vs Series9 cleanliness
    - `series10CleanPersonalUsedRows=2`
    - `series9CleanPersonalUsedRows=1`
  - Galaxy Watch7 44mm bluetooth explicit no-conflict non-merchant personal-used
    - `baseRows=2`
    - `cleanRows=0`
- 해석:
  - 현재 Apple Watch narrow lane 비교에서는 `Series10 46mm battery90+`가 `Series9 45mm battery90+`보다 clean row count가 낫다. 아직 tiny scale이지만, 다음 thickening 우선순위는 Series10 쪽으로 기울었다.
  - Galaxy Watch는 Watch7 anchor도 더 정직하게 좁히면 clean row가 0이므로, 여전히 positive-density family가 아니라 guardrail / blocker decomposition family로 유지해야 한다.
- 반영:
  - `parser-report-manifest-latest`: `rows=129`, `missing=0`
  - `parser-manifest-audit-latest`: `status=ok`, `checked=129`, `missing=0`
- 보류:
  - runtime parser wiring, candidate pool policy wiring, public promotion은 계속 보류한다.
  - Apple Watch도 row volume이 아직 작으므로 즉시 승격이 아니라 report-only thickening 우선순위만 조정한다.
- 다음:
  1. Apple Watch는 `Series10 46mm battery90+` 주변의 bundle/cellular baggage가 줄어드는지, clean personal-used density가 더 붙는지 추가 packet으로 본다.
  2. Galaxy Watch는 positive thickening보다 `why clean lane collapses`를 더 명시하는 negative-proof packet을 우선한다.

## v2.242 - camera A7M3 second no-write live-read wave completed

- 결정: camera A7M3는 두 번째 no-write live-read wave를 실행했지만 clean body-only row가 여전히 부족하므로 blocked 상태를 유지한다.
- 생성/수정:
  - `scripts/report-camera-a7m3-second-live-read-wave.ts`
  - `reports/camera-a7m3-second-live-read-wave-latest.md/json`
  - `scripts/report-camera-sony-a7m3-owner-review-packet.ts`
  - `scripts/report-camera-sony-a7m3-execution-readiness-checklist.ts`
  - `reports/camera-sony-a7m3-owner-review-packet-latest.md/json`
  - `reports/camera-sony-a7m3-execution-readiness-checklist-latest.md/json`
  - `reports/acquisition-owner-readiness-board-latest.md/json`
- 결과:
  - second wave: `searchRowsRead=12`, `detailRowsRead=12`, `freshLiveCandidates=2`, `manualReviewRows=3`, `holdRows=7`
  - unique clean body-only row는 기존과 합쳐도 `2`로, owner-ready 최소 기준 `4`에 미달한다.
  - camera checklist: `passChecks=6`, `warningChecks=1`, `failedChecks=1`
- 해석:
  - A7M3는 공식 source와 body-only positive row가 있지만, 시장 live row가 얇고 reserved/sold/buying/타모델/액세서리 오염이 커서 지금 승격하면 위험하다.
  - 억지로 runtime/후보풀에 넣는 것은 후보팩 신뢰를 깎을 가능성이 크다.
- 보류:
  - camera A7M3 internal-only executor, runtime wiring, candidate pool/public promotion은 계속 보류한다.
  - `30일_실행계획.md` append는 다른 main-agent 동시 편집 충돌 방지를 위해 계속 보류한다.
- 다음:
  1. ready 상태인 monitor/headphone/speaker는 P0 안정화 이후 owner 승인 전까지 dormant 유지.
  2. 다음 report-only 확장은 robot vacuum 또는 game console처럼 아직 blocked/risk가 큰 카테고리보다, clean row 확보 가능성이 큰 adjacent tech lane을 우선 탐색한다.

## v2.243 - acquisition ranking and board refreshed after speaker/camera updates

- 결정: speaker Flip 6는 exact-contract backfill 이후 owner-review-ready로 반영하고, camera A7M3는 second wave 이후에도 blocked로 명확히 유지했다.
- 재생성:
  - `reports/next-acquisition-readiness-ranking-latest.md/json`
  - `reports/acquisition-owner-readiness-board-latest.md/json`
- 현재 owner-ready target:
  1. `monitor_discovered / benq-zowie-xl2540k` (`rows=6`, `cap=5`)
  2. `headphone_discovered / sony-wh-1000xm4, sony-wh-ch520` (`rows=7`, `cap=7`)
  3. `speaker_audio_discovered / jbl-flip-6` (`rows=3`, `cap=3`)
- 현재 blocked target:
  - `camera_discovered / sony-a7m3` (`rows=2`, second wave 이후에도 최소 4 clean row 미달)
- 보류:
  - 모든 ready target은 P0 Supabase/runtime 안정화 및 owner 승인 전까지 dormant 상태다.
  - public promotion, runtime catalog apply, candidate pool wiring, DB executor는 계속 보류한다.
- 다음:
  1. adjacent tech lane 중 clean row 확보 가능성이 높은 후보를 report-only로 선별한다.
  2. camera는 당장 더 밀기보다 새로운 live-read source/alias 전략이 잡힐 때까지 blocked 유지한다.

## v2.244 - packet artifact helper extracted for registry-first compiler path

- 결정: `report-packet-registry.ts`에서 `script -> report json/md -> manifest md` 파생 규칙을 `compilePacketArtifacts()` / `compileLatestPhaseFiles()` / `compileManifestFiles()`로 분리했다.
- 생성/수정:
  - `scripts/lib/report-packet-registry.ts`
  - `scripts/report-parser-registry-compiler-candidate.ts`
  - `reports/parser-registry-compiler-candidate-latest.md/json`
  - `reports/parser-report-manifest-latest.md/json`
  - `reports/parser-manifest-audit-latest.md/json`
- 결과:
  - packet identity가 basename 규칙에서 공용 helper로 승격됐다.
  - `manifest audit status=ok; checked=129; missing=0` 유지.
- 해석:
  - generic runner로 급하게 뛰기 전에 가장 반복이 심한 packet 산출물 파생 규칙을 먼저 공통화해서 리스크를 낮췄다.
  - 지금 구조는 검증 품질은 유지하면서 orchestration 수동성을 한 단계 더 줄이는 방향으로 잘 가고 있다.
- 보류:
  - full generic packet runner/spec compiler는 아직 보류한다.
  - runtime wiring/public promotion/DB executor는 계속 보류한다.
- 다음:
  1. `parser-next-work-queue`의 우선순위 접착제를 helper/spec 쪽으로 더 줄일 수 있는지 본다.
  2. 필요하면 registry backlog signal과 queue row를 직접 연결하는 공용 context를 만든다.

## v2.245 - next-work queue priority plan extracted into spec

- 결정: `report-parser-next-work-queue.ts`에 인라인으로 있던 category별 priority/next-task plan을 `scripts/lib/report-next-work-plan-spec.ts`로 분리했다.
- 생성/수정:
  - `scripts/lib/report-next-work-plan-spec.ts`
  - `scripts/report-parser-next-work-queue.ts`
  - `reports/parser-next-work-queue-latest.md/json`
  - `reports/parser-category-context-status-latest.md/json`
- 결과:
  - queue orchestration의 큰 수동 블록 하나가 분리돼서 다음 queue/context 리팩터링 진입점이 더 선명해졌다.
  - `next work queue: rows=7, evidence_completed=7` 유지.
- 해석:
  - 검증 로직은 유지하면서도, 이제 queue 정책 변경은 spec 파일에서 다루고 report 본문은 조립 역할에 더 가까워졌다.
- 보류:
  - queue 우선순위를 readiness/registry signals로 완전 자동 계산하는 단계는 아직 보류한다.
- 다음:
  1. queue 우선순위 spec과 registry backlog signals 사이에 공용 mapping/helper를 둘 수 있는지 본다.
  2. full generic runner 대신 queue/context 접착제부터 더 줄인다.

## v2.244 - desktop Mac mini M2 adjacent lane explored report-only

- 결정: camera A7M3가 여전히 thin/blocked이므로, 다음 adjacent tech lane으로 `desktop_pc_discovered / Apple Mac mini M2 family`를 no-write live-read로 탐색했다.
- 생성:
  - `scripts/report-desktop-macmini-m2-live-read-wave.ts`
  - `reports/desktop-macmini-m2-live-read-wave-latest.md/json`
  - `scripts/report-desktop-macmini-m2-source-backfill.ts`
  - `reports/desktop-macmini-m2-source-backfill-latest.md/json`
  - `scripts/report-desktop-macmini-m2-owner-assessment.ts`
  - `reports/desktop-macmini-m2-owner-assessment-latest.md/json`
- 결과:
  - live-read: `searchRowsRead=12`, `detailRowsRead=12`, `freshLiveCandidates=4`, `manualReviewRows=6`, `holdRows=2`
  - official source rows: `3`
  - comparable keys: `4`, largest comparable-key row count: `1`
  - conclusion: `desktop_macmini_m2_owner_assessment_promising_but_key_rows_thin_report_only`
- 해석:
  - Mac mini M2 계열은 시장 row와 공식 근거가 있어 promising하다.
  - 다만 M2 / M2 Pro, RAM, SSD가 모두 가격-critical axis라서 4개 fresh row를 한 시세 lane으로 합치면 안 된다.
  - `RAM16GB`의 `m1` 부분을 M1 칩으로 오인하는 정규식 함정을 발견해 report-only evaluator에서 토큰 경계 방식으로 수정했다.
- 보류:
  - Mac mini M2 owner packet/executor/runtime wiring/public promotion은 보류한다.
  - `30일_실행계획.md` append는 다른 main-agent 동시 편집 충돌 방지를 위해 계속 보류한다.
- 다음:
  1. Mac mini는 broad family가 아니라 `M2 16/512` 또는 `M2 Pro 16/512` 같은 exact key별 targeted no-write wave로 두껍게 만든다.
  2. P0 안정화 전에는 어떤 desktop row도 DB/candidate pool에 쓰지 않는다.

## v2.245 - desktop Mac mini M2 targeted wave added

- 결정: broad Mac mini M2 live-read는 promising하지만 comparable key가 너무 갈라져 있어, 같은 report-only runner에 `MACMINI_TARGETED=1` 모드를 추가해 exact key 중심으로 다시 읽었다.
- 수정/생성:
  - `scripts/report-desktop-macmini-m2-live-read-wave.ts`에 targeted mode 추가
  - `reports/desktop-macmini-m2-targeted-live-read-wave-latest.md/json`
  - `scripts/report-desktop-macmini-m2-owner-assessment.ts`
  - `reports/desktop-macmini-m2-owner-assessment-latest.md/json`
- 결과:
  - targeted wave: `searchRowsRead=15`, `detailRowsRead=15`, `freshLiveCandidates=4`, `manualReviewRows=1`, `holdRows=10`, `comparableKeys=2`
  - combined unique fresh rows: `6`
  - largest comparable-key rows: `2`
  - 결론: `desktop_macmini_m2_owner_assessment_promising_but_key_rows_thin_report_only`
- 해석:
  - Mac mini M2 / M2 Pro 16GB 512GB lane은 실제 수요와 row가 보이지만, 아직 한 comparable key당 row가 4개 미만이라 owner-ready는 아니다.
  - sold/reserved row가 다수 보여 회전 신호는 있으나, 후보 공개/DB write로 넘기기 전에는 같은 key row를 더 모아야 한다.
- 보류:
  - Mac mini M2 runtime/parser/candidate pool/public promotion/DB executor는 모두 보류한다.
  - `30일_실행계획.md` append는 다른 main-agent 동시 편집 충돌 방지를 위해 계속 보류한다.
- 다음:
  1. P0 안정화 대기 중 report-only로는 Mac mini exact key를 한 번 더 얇게 보거나, 다른 adjacent tech lane을 같은 방식으로 탐색한다.
  2. owner-ready board에는 아직 Mac mini를 추가하지 않는다.

## v2.246 - registry backlog signals bridged into next-work queue via helper

- 결정: registry backlog signals를 별도 표로만 덤프하지 않고, queue row가 category별 top signal을 직접 읽도록 helper를 추가했다.
- 생성/수정:
  - `scripts/lib/report-registry-backlog-context.ts`
  - `scripts/lib/report-next-work-context.ts`
  - `scripts/report-parser-registry-backlog-signals.ts`
  - `scripts/report-parser-next-work-queue.ts`
  - `scripts/report-parser-readiness-all.ts`
  - `scripts/report-parser-report-only-audit.ts`
  - `scripts/report-parser-report-manifest.ts`
  - `scripts/report-parser-manifest-audit.ts`
- 결과:
  - `parser-next-work-queue-latest`는 이제 각 row에 `registryBacklogSummary`를 직접 가진다.
  - 구조 리포트들도 `readiness-all -> manifest -> audit` 정식 체인에 포함됐다.
  - `manifest audit status=ok; checked=134; missing=0` 유지.
- 해석:
  - queue 본문은 점점 조립 역할에 가까워지고 있고, signal 계산/가공은 helper/spec 레이어로 이동 중이다.
  - 검증은 유지하면서 orchestration 접착제가 또 한 겹 줄었다.
- 보류:
  - queue priority를 registry severity/readiness로 자동 산정하는 단계는 아직 보류한다.
  - full generic packet runner는 계속 보류한다.
- 다음:
  1. `next-work priority spec`과 `registry packet group metadata` 사이의 category 매핑을 더 data-driven 하게 줄일 수 있는지 본다.
  2. 필요하면 queue row lifecycle 상태 계산도 helper/spec로 더 분리한다.

## v2.247 - queue row lifecycle materialization extracted into helper

- 결정: `parser-next-work-queue.ts` 안의 queueStatus/nextSafeTask/summary 문자열 계산을 `scripts/lib/report-next-work-row-status.ts`로 분리했다.
- 생성/수정:
  - `scripts/lib/report-next-work-row-status.ts`
  - `scripts/report-parser-next-work-queue.ts`
  - `scripts/lib/report-category-key-spec.ts`
  - `scripts/lib/report-category-editorial-spec.ts`
  - `scripts/lib/report-registry-backlog-context.ts`
- 결과:
  - queue 본문은 `base rows -> materialize -> render` 구조에 더 가까워졌다.
  - category key 매핑도 공용 spec로 모였고, `completed=7 / evidence_completed=7 / queued=0` 유지.
- 해석:
  - 구조 리팩터링이 숫자를 깨지 않고 잘 먹히고 있다.
  - 지금 남은 큰 수동성은 priority plan 자체와 registry metadata 사이의 수동 연결 정도다.
- 보류:
  - priority 자체를 readiness/registry severity로 자동 계산하는 단계는 아직 보류한다.
- 다음:
  1. priority plan이 registry group/tag/phase와 더 직접 연결될 수 있는지 본다.
  2. 그 다음 full generic runner가 아니라, plan/metadata mapping을 더 줄이는 쪽으로 간다.

## v2.248 - queue category now carries explicit registry group linkage

- 결정: `next-work queue`가 registry backlog를 category 문자열 정규화에만 기대지 않도록, `discovered category -> registry group key` 연결을 공용 spec에 명시했다.
- 생성/수정:
  - `scripts/lib/report-category-key-spec.ts`
  - `scripts/lib/report-registry-backlog-context.ts`
  - `scripts/lib/report-next-work-context.ts`
  - `scripts/lib/report-next-work-row-status.ts`
  - `scripts/report-parser-next-work-queue.ts`
- 결과:
  - queue row는 이제 `registryGroupKey`를 직접 가진다.
  - registry backlog top signal은 먼저 explicit group key로 찾고, 필요할 때만 category 정규화 fallback을 탄다.
  - 최신 queue도 그대로 유지:
    - `rows=7`
    - `completed=7`
    - `evidence_completed=7`
    - `queued=0`
- 해석:
  - 이제 queue는 “이 category가 어떤 registry family/group을 보고 있는가”를 명시적으로 드러낸다.
  - 다음 단계에서 priority spec을 registry metadata와 더 data-driven 하게 묶기 쉬워졌다.
- 보류:
  - priority 점수를 registry severity에서 자동 계산하는 단계는 아직 보류한다.
  - full generic packet runner는 계속 보류한다.
- 다음:
  1. `nextWorkPriorityPlan`이 registry group metadata를 직접 참조하는 얇은 bridge spec/helper를 검토한다.
  2. queue priority row에서 category/group 단위 narrative를 더 줄일 수 있는지 본다.

## v2.249 - next-work priority plan now resolves shared registry metadata

- 결정: `nextWorkPriorityPlan`을 읽을 때 `category -> registryCategory -> registryGroupKey`를 한 번에 풀어주는 resolver를 추가해, queue 조립부가 category 문자열과 registry bridge를 따로 들고 있지 않게 했다.
- 생성/수정:
  - `scripts/lib/report-next-work-plan-spec.ts`
  - `scripts/lib/report-next-work-context.ts`
- 결과:
  - queue row는 이제 `registryCategory`와 `registryGroupKey`를 같이 가진다.
  - 최신 queue도 그대로 유지:
    - `rows=7`
    - `completed=7`
    - `evidence_completed=7`
    - `queued=0`
  - sample:
    - `monitor_discovered -> registryCategory=monitor -> registryGroupKey=monitor-modelcode`
    - `desktop_pc_discovered -> registryCategory=desktop -> registryGroupKey=desktop-fullunit`
- 해석:
  - plan layer가 registry metadata와 한 단계 더 직접 연결됐다.
  - 다음엔 priority narrative/spec을 family/group metadata 기반으로 더 줄이기 쉬워졌다.
- 보류:
  - priority 산정 자동화는 계속 보류한다.
  - queue priority를 registry severity로 재정렬하는 단계도 아직 보류한다.
- 다음:
  1. `nextWorkPriorityPlan` 설명문 일부가 registry group/family metadata를 재사용할 수 있는지 본다.
  2. 필요하면 queue markdown 렌더도 group/family 단위 가독성 쪽으로 조금 더 줄인다.

## v2.250 - next-work plan resolver now carries registry family metadata

- 결정: `nextWorkPriorityPlan` resolver가 `registryGroupKey`에서 멈추지 않고, `registryFamily / registryPhase / registryTags`까지 함께 반환하도록 올렸다.
- 생성/수정:
  - `scripts/lib/report-packet-registry.ts`
  - `scripts/lib/report-next-work-plan-spec.ts`
- 결과:
  - queue row sample은 이제 다음을 함께 가진다:
    - `monitor_discovered -> monitor-modelcode -> monitor model code -> boundary`
    - `desktop_pc_discovered -> desktop-fullunit -> desktop full-unit -> boundary`
  - 최신 queue 값은 그대로 유지:
    - `rows=7`
    - `completed=7`
    - `evidence_completed=7`
    - `queued=0`
- 해석:
  - queue priority layer가 registry family 메타데이터를 직접 사용할 준비가 됐다.
  - 다음엔 plan 설명문/queue 렌더에서 family/phase를 재활용해 중복 서술을 더 줄이기 쉬워졌다.
- 보류:
  - family/phase 기반 priority 자동 재정렬은 아직 보류한다.
  - full generic queue renderer는 아직 보류한다.
- 다음:
  1. queue markdown/report 서술이 registry family metadata를 재사용할 수 있는지 본다.
  2. 가능하면 priority plan prose의 반복 표현을 family/group 기준 helper로 더 줄인다.

## v2.251 - next-work queue rendering extracted and family scope surfaced

- 결정: `parser-next-work-queue.ts` 안의 markdown/table 렌더 블록을 helper로 분리하고, queue 표에 `registry_scope`를 정식 컬럼으로 노출했다.
- 생성/수정:
  - `scripts/lib/report-next-work-render.ts`
  - `scripts/lib/report-next-work-row-status.ts`
  - `scripts/report-parser-next-work-queue.ts`
- 결과:
  - queue 본문은 이제 데이터 로드/조립/저장에 더 가깝고, 렌더링은 helper가 맡는다.
  - markdown 표는 `category` 옆에 `registry_scope`를 직접 보여준다.
  - 최신 queue 값은 그대로 유지:
    - `rows=7`
    - `completed=7`
    - `evidence_completed=7`
    - `queued=0`
- 해석:
  - 이제 queue 쪽 남은 수동성은 렌더링 코드보다 plan prose/content 쪽에 더 가깝다.
  - `registry_scope`가 직접 보이면서 family/phase 단위 판단이 더 쉬워졌다.
- 보류:
  - priority prose를 registry metadata에서 반자동 생성하는 단계는 아직 보류한다.
  - queue priority 자동 재정렬도 계속 보류한다.
- 다음:
  1. `nextWorkPriorityPlan`의 반복 서술을 group/family helper로 더 줄일 수 있는지 본다.
  2. 가능하면 queue JSON에도 row-level family/phase narrative를 더 정리된 필드로 내보낸다.

## v2.252 - next-work static queue prose/spec extracted

- 결정: `parser-next-work-queue.ts`에 남아 있던 고정 문구와 모드 선언(`reportOnly`, `guideHintsReferenceReport`, `notQueuedReason`, `guardrails`)을 별도 static spec로 분리했다.
- 생성/수정:
  - `scripts/lib/report-next-work-static-spec.ts`
  - `scripts/report-parser-next-work-queue.ts`
- 결과:
  - queue 본문에는 이제 고정 모드/가드레일/미큐잉 사유가 거의 남지 않는다.
  - 최신 queue 값은 그대로 유지:
    - `rows=7`
    - `completed=7`
    - `evidence_completed=7`
    - `queued=0`
    - `guideHintsReferenceReport=model-guide-parser-hints-latest.md`
- 해석:
  - queue script는 점점 `sources load -> row build -> render -> persist` 흐름만 남는 상태가 됐다.
  - 남은 수동성은 고정 prose보다 `nextWorkPriorityPlan` 본문 자체에 더 많이 모여 있다.
- 보류:
  - priority prose 자동 생성/압축은 계속 보류한다.
  - queue priority 자동 재정렬도 계속 보류한다.
- 다음:
  1. `nextWorkPriorityPlan` 본문 중 반복되는 narrative 패턴을 helper/spec로 더 줄일 수 있는지 검토한다.
  2. 아니면 이 구조 개선 wave를 여기서 닫고, 다시 wearable evidence thickening 쪽으로 복귀한다.

## v2.250 - acquisition board refreshed with PS5 Disc/Digital report-only lane

- 결정: `game_console_body_narrow`의 PS5 Disc/Digital lane을 no-write live-read, 공식 source backfill, owner assessment, execution checklist까지 연결해 acquisition board에 반영했다.
- 생성/수정:
  - `scripts/report-game-console-ps5-live-read-wave.ts`
  - `scripts/report-game-console-ps5-source-backfill.ts`
  - `scripts/report-game-console-ps5-owner-assessment.ts`
  - `scripts/report-game-console-ps5-execution-readiness-checklist.ts`
  - `scripts/report-next-acquisition-readiness-ranking.ts`
  - `scripts/report-acquisition-owner-readiness-board.ts`
  - `reports/game-console-ps5-*.{md,json}`
  - `reports/next-acquisition-readiness-ranking-latest.{md,json}`
  - `reports/acquisition-owner-readiness-board-latest.{md,json}`
- 결과:
  - `game_console_body_narrow / playstation-5-disc, playstation-5-digital`이 report-only 기준 owner-review-ready 1순위로 올라왔다.
  - 최종 board:
    - owner-review-ready: PS5 Disc/Digital, BenQ ZOWIE XL2540K, Sony WH-1000XM4/CH520, JBL Flip 6
    - blocked: Sony A7M3
  - PS5 live read는 `freshLiveCandidates=16`, owner assessment는 Disc/Digital clean rows `11`, future write cap `8`, checklist `pass=8 / warn=0 / fail=0`.
- 해석:
  - PS5는 row volume과 clean split이 좋아 다음 acquisition 후보로 강하다.
  - 다만 Slim/Pro/limited/accessory drive/game/account bundle은 아직 보수적으로 제외/수동검토해야 한다.
  - 첫 실제 실행 후보는 숫자상 PS5가 강하지만, 가장 보수적인 첫 lane은 여전히 모니터 XL2540K다.
- 보류:
  - runtime parser wiring, candidate pool wiring, public promotion, DB write는 계속 보류한다.
  - PS5 Slim lane은 row가 promising이지만 Disc/Digital과 분리해서 별도 thickening 후 판단한다.
- 다음:
  1. 다른 main agent의 P0 Supabase/runtime 안정화가 끝난 뒤, owner-review-ready 4개 lane 중 어떤 순서로 narrow runtime patch를 검토할지 결정한다.
  2. 그 전까지는 no-write live-read/source-backfill/report-only 산출물만 계속 보강한다.

## v2.251 - PS5 runtime review request draft added

- 결정: PS5 Disc/Digital lane이 owner-review-ready로 올라왔지만, 바로 runtime patch로 넘어가지 않도록 별도 승인 요청 초안을 만들었다.
- 생성:
  - `scripts/report-game-console-ps5-runtime-review-request-draft.ts`
  - `reports/game-console-ps5-runtime-review-request-draft-latest.{md,json}`
- 결과:
  - target: `playstation-5-disc, playstation-5-digital`
  - allowedRows: `11`
  - maxFutureWriteCap: `8`
  - requested scope/non-scope/approval checklist를 명시했다.
- 해석:
  - 이제 PS5도 headphone처럼 “report-only 준비 완료”와 “runtime 승인” 사이 경계가 문서로 분리됐다.
  - PS5 Slim/Pro/limited/accessory/game/account bundle은 계속 deferred/hold로 남는다.
- 보류:
  - runtime parser patch, candidate pool wiring, public promotion, DB write는 계속 보류한다.
- 다음:
  1. owner-review-ready lane들에 같은 approval-boundary packet이 빠진 곳이 있는지 확인한다.
  2. 그 다음 새로운 tech/electronics category no-write live-read를 계속 확장한다.

## v2.252 - acquisition approval boundary matrix added

- 결정: owner-review-ready lane마다 승인 경계가 제각각 흩어지는 것을 막기 위해 acquisition board 위에 approval-boundary matrix를 추가했다.
- 생성:
  - `scripts/report-acquisition-approval-boundary-matrix.ts`
  - `reports/acquisition-approval-boundary-matrix-latest.{md,json}`
- 결과:
  - ownerReviewReadyRows: `4`
  - missingHardBoundaryRows: `0`
  - missingRuntimeRequestRows: `2`
  - PS5/Headphone은 runtime-review request draft까지 있고, Monitor/Speaker는 owner packet + execution checklist는 있으나 selected 시 runtime-review request draft를 추가하면 된다.
- 해석:
  - runtime patch 전 필수 하드 경계(owner packet + checklist)는 4개 ready lane 모두 충족한다.
  - monitor/speaker는 실행 후보로 고를 때만 request draft를 추가하면 되므로 지금 당장 blocker는 아니다.
- 보류:
  - runtime patch와 public promotion은 계속 보류한다.
- 다음:
  1. 신규 category no-write live-read를 이어가되, owner-ready로 올라오는 순간 이 matrix에 자동 반영되도록 유지한다.
  2. runtime 선택 직전에는 선택된 lane만 request draft를 생성한다.

## v2.253 - iPad Pro 11 M4 tablet lane opened as promising, not ready

- 결정: 태블릿은 broad category 승격 대신 `iPad Pro 11 M4` 정확 lane만 no-write live-read로 열어 위험도를 확인했다.
- 생성/수정:
  - `scripts/report-tablet-ipad-pro-m4-live-read-wave.ts`
  - `scripts/report-tablet-ipad-pro-m4-source-backfill.ts`
  - `scripts/report-tablet-ipad-pro-m4-owner-assessment.ts`
  - `scripts/report-next-acquisition-readiness-ranking.ts`
  - `reports/tablet-ipad-pro-m4-*.{md,json}`
  - `reports/next-acquisition-readiness-ranking-latest.{md,json}`
- 결과:
  - live read: `searchRowsRead=24`, `detailRowsRead=24`, `freshLiveCandidates=4`, `manualReviewRows=13`, `holdRows=7`, `comparableKeys=3`.
  - owner assessment: `ownerReadyKeys=0`, `promisingKeys=1`.
  - strongest key는 256GB Wi-Fi 쪽 2 rows라 아직 owner-ready 기준에는 부족하다.
- 해석:
  - 태블릿은 가능성이 있지만 storage/connectivity split이 강해서 “fresh 전체 개수”만 보면 위험하다.
  - Wi-Fi/Cellular, 256GB/512GB/1TB를 절대 합치면 안 되고, Pencil/Keyboard/Case/Film/Pouch/plus context는 수동검토로 유지해야 한다.
- 보류:
  - tablet runtime patch, candidate pool wiring, public promotion은 보류한다.
  - iPad Pro 13 M4, iPad Air M4는 별도 lane으로 분리 전까지 보류한다.
- 다음:
  1. `iPad Pro 11 M4 256GB Wi-Fi`와 `256GB Cellular`를 target으로 두 번째 no-write wave를 돌린다.
  2. key별 4 rows 이상이 쌓일 때만 execution checklist로 넘어간다.

## v2.254 - iPad Pro 11 M4 256GB Wi-Fi became owner-review-ready after targeted wave

- 결정: iPad Pro 11 M4 첫 wave의 promising 상태를 확인한 뒤, 256GB Wi-Fi/Cellular targeted no-write wave를 추가로 돌려 key density를 다시 평가했다.
- 생성/수정:
  - `scripts/report-tablet-ipad-pro-m4-live-read-wave.ts`
  - `scripts/report-tablet-ipad-pro-m4-owner-assessment.ts`
  - `scripts/report-tablet-ipad-pro-m4-execution-readiness-checklist.ts`
  - `scripts/report-next-acquisition-readiness-ranking.ts`
  - `scripts/report-acquisition-owner-readiness-board.ts`
  - `scripts/report-acquisition-approval-boundary-matrix.ts`
  - `reports/tablet-ipad-pro-m4-targeted-live-read-wave-latest.{md,json}`
  - `reports/tablet-ipad-pro-m4-execution-readiness-checklist-latest.{md,json}`
- 결과:
  - targeted wave: `freshLiveCandidates=5`, `manualReviewRows=14`, `holdRows=5`, `comparableKeys=2`.
  - combined owner assessment: `ownerReadyKeys=1`.
  - execution checklist: `pass=8 / warn=0 / fail=0`, `allowedRows=4`, `maxFutureWriteCap=4`.
  - acquisition owner board가 6 entries로 갱신됐고, owner-review-ready rows는 5개가 됐다.
- 해석:
  - iPad Pro 11 M4는 전체 태블릿 category가 아니라 단일 `storage/connectivity` key로만 안전하게 다룰 수 있다.
  - 첫 pass는 single key만 허용해야 하며, 13-inch M4 / iPad Air M4 / Pencil / Keyboard / Case bundle은 계속 제외한다.
- 보류:
  - tablet runtime patch, candidate pool wiring, public promotion은 계속 보류한다.
  - iPad Pro 11 M4의 다른 storage/connectivity key는 별도 row density가 쌓일 때까지 보류한다.
- 다음:
  1. 선택된 runtime lane이 있다면 해당 lane만 runtime-review request draft를 생성한다.
  2. 그 전까지는 다른 테크/전자 exact lane을 같은 no-write 방식으로 확장한다.

## v2.255 - iPhone 15 Pro no-write wave confirms smartphone should stay internal-only

- 결정: 스마트폰 category를 broad하게 열지 않고, `iPhone 15 Pro 256GB` 중심 no-write live-read로 파서/시장 오염도를 확인했다.
- 생성:
  - `scripts/report-smartphone-iphone15pro-live-read-wave.ts`
  - `reports/smartphone-iphone15pro-live-read-wave-latest.{md,json}`
- 결과:
  - `searchRowsRead=25`, `detailRowsRead=24`
  - `freshLiveCandidates=2`, `manualReviewRows=9`, `holdRows=13`, `comparableKeys=1`
  - clean key는 `smartphone|apple_iphone_15_pro|256|unlocked|bat95plus` 2 rows뿐이다.
- 해석:
  - 스마트폰은 배터리효율, 자급제/통신사, Pro/Pro Max 혼입, 예약/구매/렌탈/파손/애케플 문맥 때문에 아직 공개 후보팩으로 바로 열면 위험하다.
  - 현재 판단은 기존 `Mobile Phone internal_only` 결정을 강화한다.
- 보류:
  - iPhone runtime patch, candidate pool wiring, public promotion은 보류한다.
  - 배터리효율 없는 row를 AI로 승격시키는 것도 아직 보류한다.
- 다음:
  1. 스마트폰을 계속 볼 경우 `iPhone 15 Pro 256 자급제 bat95plus`처럼 battery/network key를 고정한 targeted wave가 먼저다.
  2. 당장 acquisition 확장은 PS5/Monitor/Headphone/Speaker/iPad처럼 owner-ready lane이 있는 쪽을 우선한다.

## v2.256 - next-work priority plan repetition reduced with blueprint builders

- 결정: `nextWorkPriorityPlan`의 남은 수동 반복을 줄이기 위해 plan spec 안에 `stage()`, `candidatePrecheckBlueprint()`, `holdDiagnosisBlueprint()` 빌더를 추가했다.
- 생성/수정:
  - `scripts/lib/report-next-work-plan-spec.ts`
  - `reports/parser-next-work-queue-latest.{md,json}`
- 결과:
  - queue 출력은 그대로 유지됐다: `rows=7`, `queued=0`, `completed=7`, `evidence_completed=7`
  - plan spec은 이제 `workType`와 stage object 반복을 helper로 흡수하고, entry별 의도 문구만 남기는 구조가 됐다.
- 해석:
  - “거대한 class base로 전부 통합” 같은 급격한 프레임워크화보다, 현재 구조에서는 `spec/helper/registry`를 한 겹씩 올리면서 반복도를 줄이는 방식이 더 안전하다.
  - 이번 단계는 출력 안정성을 유지한 채 prose-heavy plan 정의를 얇게 만든 작은 리팩터링이다.
- 보류:
  - `nextWorkPriorityPlan`의 긴 narrative 자체를 registry metadata에서 자동 합성하는 단계는 아직 보류한다.
  - 런타임 parser, candidate pool, public promotion wiring은 여전히 보류한다.
- 다음:
  1. `nextWorkPriorityPlan`의 반복 서술 중 family/phase 공통 문구를 더 helper/spec로 뺄 수 있는지 본다.
  2. 그다음 다시 `smartwatch-wearables` evidence thickening / guardrail decomposition 흐름으로 복귀한다.

## v2.257 - repo hygiene immediate guardrails applied

- 결정: 즉시 충돌 위험이 낮고 효과가 큰 hygiene 조치만 먼저 적용했다.
- 생성/수정:
  - `mvp/.gitignore`
- 적용:
  - `reports/`
  - `.codex-backups/`
  를 ignore 대상으로 추가했다.
- 해석:
  - `reports/`는 generated 산출물 비중이 높아서 기본적으로 작업 트리 소음을 줄이는 쪽이 맞다.
  - `.codex-backups/`는 로컬 복구용 산출물이므로 git 대상에서 빼는 것이 맞다.
- 보류:
  - 이미 tracked된 report 파일의 cached 정리는 이번 wave에서 하지 않았다.
  - `category-intelligence/**/*.json` 전체를 ignore/Supabase externalize 하는 건 보류한다.
  - 이유: `category-intelligence`에는 generated JSON과 curated knowledge(`sku_catalog.json`, `noise_rules.json`, `shared_knowledge.json`, 각종 REVIEW/PROMOTION 문서)가 섞여 있어서 통짜 처리하면 위험하다.
- 다른 에이전트 메모:
  - `category-intelligence`는 **generated vs curated 분류표를 먼저 만든 뒤** selective ignore / externalize를 결정해야 한다.
  - `category-intelligence JSON 전체 = 캐시`라고 가정하면 안 된다.
- 다음:
  1. 다른 wave에서 `category-intelligence`의 generated/curated 분류표를 먼저 만든다.
  2. 그 다음에만 selective ignore / externalize 범위를 확정한다.

## v2.258 - next-work plan passive stages and stop conditions extracted

- 결정: `nextWorkPriorityPlan`의 남은 반복 prose 중 구조적으로 가장 반복이 큰 `빈 task stage`와 `stopCondition` 문구를 helper/상수로 분리했다.
- 생성/수정:
  - `scripts/lib/report-next-work-plan-spec.ts`
  - `reports/parser-next-work-queue-latest.{md,json}`
- 적용:
  - `passiveStage()` helper 추가
  - `stopConditions` 상수 묶음 추가
  - 각 category blueprint에서 `stage("", ...)` 반복과 stop-condition literal 반복을 제거
- 결과:
  - queue 출력 유지: `rows=7`, `queued=0`, `completed=7`, `evidence_completed=7`
  - plan spec은 이제 category별 차이 문구에 더 집중하고, 공통적인 “수동 접착제”는 helper로 흡수하는 구조가 됐다.
- 해석:
  - 감사보고서의 “동적 러너/공용 프레임워크” 문제 제기는 방향성은 맞지만, 현재 프로젝트에서는 이런 식의 점진적 helper 추출이 더 안전하다.
  - 이번 단계는 큰 구조를 또 바꾸지 않고, 이미 얇아진 queue/spec 레이어를 더 읽기 좋게 만드는 정리다.
- 보류:
  - `nextWorkPriorityPlan`의 category별 긴 narrative 본문 전체를 템플릿화/자동합성하는 건 아직 보류한다.
  - `category-intelligence` 정리는 generated/curated 분류표 선행 전까지 보류한다.
- 다음:
  1. `nextWorkPriorityPlan` 안의 category별 긴 narrative에서 공통 policy 문장을 더 helper/spec로 줄일 수 있는지 본다.
  2. 그다음 구조 개선 wave를 닫고 `smartwatch-wearables` evidence thickening으로 복귀한다.

## v2.257 - acquisition runtime review ordering packet added

- 결정: owner-ready lane이 5개로 늘어났기 때문에, “숫자상 1순위”와 “실제 첫 runtime review로 가장 안전한 후보”를 분리해서 보는 ordering packet을 추가했다.
- 생성:
  - `scripts/report-acquisition-runtime-review-ordering-packet.ts`
  - `reports/acquisition-runtime-review-ordering-packet-latest.{md,json}`
- 결과:
  - readyRows: `5`
  - blockedRows: `1`
  - firstRecommended: `benq-zowie-xl2540k`
  - 숫자상 최고 후보는 PS5 Disc/Digital이지만, 첫 runtime review rehearsal은 모니터 XL2540K가 가장 안전하다고 정리했다.
- 해석:
  - PS5는 volume이 강하지만 edition/bundle/accessory 경계가 넓다.
  - Monitor는 row 수는 더 적어도 exact model code surface가 가장 좁아서 첫 검증 후보로 적합하다.
  - iPad는 owner-ready지만 single storage/connectivity key만 허용해야 한다.
- 보류:
  - P0 Supabase/runtime 안정화 전 runtime patch는 계속 보류한다.
  - ordering packet은 실행 승인 문서가 아니라 실행 순서 판단 문서다.
- 다음:
  1. P0 안정화 후 owner가 runtime review를 승인하면 `monitor_discovered / benq-zowie-xl2540k`부터 request draft를 만들고 tiny internal-only rehearsal을 검토한다.
  2. 그 전에는 no-write exact lane 확장을 계속한다.
