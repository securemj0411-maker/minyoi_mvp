# 2026-05-20 Detail Scrap Save

## 결정

- 상세 모달의 `<`/홈 아이콘과 같은 높이 오른쪽 끝에 스크랩 저장 버튼을 추가했다.
- 사진이 보일 때는 이미지 위 floating 버튼, 사진이 지나가면 sticky nav 오른쪽 버튼으로 같은 위치감을 유지한다.
- `/me`에서 열린 상세는 새 스키마를 만들지 않고 기존 `mvp_reveal_feedback.feedback_type = "watching"`을 스크랩 저장 신호로 재사용한다.
- `/explore`처럼 별도 서버 저장 콜백이 없는 상세는 브라우저 `localStorage`에 `pid`를 저장해 즉시 상태를 유지한다.

## 보류

- 스크랩만 모아보는 별도 `/me` 필터/탭은 이번 범위에서 제외했다.
- 새 `saved_at` 컬럼/전용 테이블은 PG 승인 전 운영 리스크를 줄이기 위해 보류했다.
