# 2026-06-05 Wave 1147 — 새매물 알림 링크 스크롤 초기화

## 결정
- 피드 하단의 `새매물 알림 받기`를 누르면 알림 화면 상단에서 시작하게 한다.

## 구현
- `src/components/explore-client.tsx`
  - `/me?view=hotdeal-alerts` 링크의 `scroll={false}`를 제거했다.

## 원인
- 피드 하단에서 알림 링크를 누르면 기존 피드 스크롤 위치가 유지됐다.
- 같은 `/me` 라우트의 query만 바뀌는 형태라 알림 화면이 아래쪽 위치에서 열리고, 사용자는 `동작 방식` 같은 하단 문구만 보게 됐다.

## 보류
- 알림 화면 내부의 별도 anchor/탭 이동 UX는 이번 범위에서 제외했다.
