# Wave 1229 — 비회원 랜딩 전환율: 돈 후크 헤드라인 + 호기심 갭 샘플 게이트

날짜: 2026-06-08 (KST)
계기: owner — "광고 17클릭인데 가입 0명. 랜딩이 별론가? sample 다 보여서 '봤다'고 느끼고 나가는 듯. 가입 자체가 안 되는 것 같기도."

## 진단 — 코드/DB는 멀쩡 (먼저 확인)
- 신규 가입(auth.users): 3일 **0**, 14일 **1** (마지막 05-30). 반면 멤버십 신청 7일 **62건**(=기존 회원). → 신규 가입만 ~2주 정전, 17클릭 0가입과 일치.
- 가입 플로우 코드 검토(auth-form / 카카오 OAuth / `/auth/callback` / 트리거): **이상 없음**. auth.users 트리거 **0개**(실패할 트리거 자체가 없음). auth 로그 24h 전부 200, "Database error saving new user" 0, 신규 OAuth 교환 흔적 0.
- 결론: **코드 정상.** 0가입 원인 후보 = (a) **카카오 콘솔 설정**(앱 개발모드 / Redirect URI 미등록 — 신규(미등록) 카카오 계정만 차단, 우리 로그엔 무흔적) 또는 (b) 랜딩→로그인 클릭 전환 약함. → **owner에 시크릿창 신규 카카오 가입 테스트 요청 (미해결, 진짜 병목 후보).**

## 랜딩 전환 개선 (이번 변경)
대상: `src/components/preview-masked-dashboard-server.tsx` (비로그인 메인 우측 피처).
실데이터 근거(ready pool): 총 4,202 / 평균차익 36,912원 / 5만+ 931 / 10만+ 354.

1. **헤드라인 돈 후크 + 숫자 가시성**: "돈 되는 중고 상품만 추천"(막연) → **"시세보다 평균 `3만원+` 싼 중고만 모아드려요"**, `3만원+`를 emerald(차익색, 카드와 통일)로 강조. ("콕" 등 어색 표현 제거.)
2. **CTA 통일**: "로그인하고 신청" / "멤버십 신청"(방문자 "뭘 신청?" 혼란) → **"지금 시작하기"**.
3. **디플레이팅 제거**: 샘플 footer "판매완료/종료된 미리보기 샘플" 삭제.
4. **호기심 갭(curiosity gap)** — owner 핵심 지적("샘플 다 보여서 누를 이유 없음") 대응:
   - 풀 공개 카드 **3개**(증거: 진짜 차익 매물 존재 → 트러스트) + 나머지(`slice(3,6)`) **블러 + 그라데이션 페이드 + 🔒 게이트**.
   - 게이트: "이런 차익 매물, 지금 수천 개가 더 있어요 / 로그인하면 **전체 매물 + 어디서 사는지(원본 링크)** 공개 / [잠금 풀고 전체 보기 →]".
   - `PreviewCard` 추출(공개/블러 공용 DRY). 블러영역 `min-h-240` (짧은 풀에서 게이트 오버플로 방지).
   - 의도: "피드 다 봤다" → **"일각만 봤고 출처(원본 링크)는 잠김"**. 완전히 안 가림 — 콜드 트래픽 트러스트 위해 증거 3개 유지, 가리는 게 아니라 *긴장*만 얹음.

## 검증
- dev:3000 핫리로드 라이브 이터레이션(owner 동석, 화면 보며 카피 조절).
- `tsc --noEmit`: 47(= baseline, 전부 tests/), 내 파일 0. base = origin/main 최신(68b23956).

## 미해결 / 후속
- **[P0] 카카오 신규 가입 가능 여부 테스트(owner)** — 0가입의 진짜 병목일 수 있음. 코드 아니면 카카오 Developers(앱 상태/Redirect URI/동의항목) + Supabase Auth(Redirect URLs 허용목록에 광고 도메인) 확인.
- 게이트 "수천 개" → 실시간 정확 숫자 동적 바인딩(선택, owner 원하면).
- `/login`의 카카오 신규가입 시 약관/개인정보/만14세 **필수동의 건너뜀**(전자상거래법/개인정보보호법 — 별도 fix 필요).
