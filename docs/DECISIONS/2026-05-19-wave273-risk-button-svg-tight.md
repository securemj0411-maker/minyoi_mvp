# 2026-05-19 wave273 — 위험 신호 버튼 SVG 아이콘화

## 결정
- 위험 신호 상세 버튼의 이모지를 제거하고 `ShieldIcon` / `AlertTriangleIcon` SVG로 바꿨다.
- `안전`, `주의 N건`, `위험 N건` 기본 라벨에서도 이모지를 제거했다.
- 버튼 높이를 `py-0.5`와 `leading-none`으로 낮춰 하단 여백처럼 보이던 공간을 줄였다.

## 보류
- 위험 점수 산식과 5축 상세 내용은 변경하지 않았다.
