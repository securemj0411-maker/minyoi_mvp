# 2026-06-04 Wave 1091 - Plans Mobile CTA / Upsell Cleanup

## 결정

- `/plans` 비회원 사이드 패널에서 `191/300`, `64%`, `선공개 예약률`을 다시 보여주던 중복 Seat check 영역을 제거했다.
- 300명 정원과 현재 예약률은 hero의 membership cap 카드 한 곳에서만 보여주고, 사이드 패널은 멤버십 신청 액션에 집중하도록 바꿨다.
- 모바일에서는 비회원 신청 CTA가 하단에 고정되도록 추가했다. 로그인 전에는 로그인 CTA, 로그인 후에는 멤버십 신청 CTA가 고정된다.
- 업셀 모달은 하단 버튼을 `기존 걸로 진행` / `특별가로 예약` 두 개로 줄이고, 다시 고르기는 우상단 X 버튼으로 이동했다.
- 업셀 카드에 정가, 10분 조건가, 할인율을 같이 보여주도록 바꿨다.

## 보류

- `/plans` 전체 비주얼 리디자인은 별도 wave에서 진행한다. 이번 작업은 광고 유입 전 혼란을 줄이는 정보 구조와 CTA 정리에 한정했다.
