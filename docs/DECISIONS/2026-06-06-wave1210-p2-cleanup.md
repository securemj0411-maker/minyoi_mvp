# Wave 1210 — P2 잔여 버그 정리 (audit)

날짜: 2026-06-06
관련: Wave 1205 audit P2, owner "남은거 검토해서 고쳐"

## fix (명확 + 안전)

1. **lookup 로그인 복귀** — lookup/page.tsx:21. `next=/plans` → `next=/lookup`.
   멤버는 로그인 후 시세조회로 복귀, 비멤버는 /lookup이 다시 /plans로 보냄(의미 보존).
2. **closeRefreshModal useCallback cleanup 오용** — explore-client.tsx:2692. `return () => clearTimeout`은
   onClick 핸들러가 버려서 무의미했음 → 반환 제거(250ms 단발이라 영향 미미, setState-on-unmounted 경고 소지 제거).
3. **telegram 폴링 영구 지속** — telegram-connect-panel.tsx:56. connected 후에도 5초 폴링 → `status?.connected`면 폴링 안 검(dep 추가).
4. **모바일 필터 시트 뒤로가기 이탈** — explore-client.tsx:2754. popstate 핸들러 추가 → 뒤로가기 시 시트만 닫고 피드 이탈 방지(새로고침 모달 패턴).

## 보류 (위험 대비 빈도 낮음 — 의도적 미수정)

- **매초 틱 무가드** (explore-client:2923): `now`가 cooldown(2932) + membership(2949) **두 countdown에 공유**돼서,
  cooldown만 보고 가드하면 membership countdown이 깨짐. 성능(매초 리렌더) 이득 < countdown 깰 위험 → **건드리지 않음**.
- **새로고침 모달 X 닫기 시 history 미회수** (explore-client:2692+2700): pushState 유령 엔트리 → 뒤로가기 1회 헛방.
  popstate 핸들러와 애니메이션 상호작용 복잡 + 빈도 낮음(헛방 1회뿐) → 보류.
- **lookup 연속조회 race** (lookup-client AbortController 부재): 페이지 라우트라 조회 중 이탈 드뭄 → 보류.
- **중첩 모달 뒤로가기 전체 닫힘** (pack-reveal 내부 레이어): 닫기 버튼 다 있어 갇힘 아님 → 보류.

## 측정 예정
- 시세 condition 이중차감 가능성 (profit.ts:86) — 시세가 이미 condition 분리됐는데 또 페널티. 영향 측정 후 판단.

## TS check
clean (4개 fix).
